# 성경 데이터 확장 가이드 — 개발자 인계 문서

> **작성일**: 2026-05-05  
> **대상**: 예봄카드 앱 개발자  
> **내용**: bible_verses 테이블에 3개 성경 버전 추가 완료 안내 및 연동 가이드

---

## 1. 변경 요약

기존 `bible_verses` 테이블에 3개 성경 버전이 추가되었습니다. 테이블 스키마 변경 없이 동일한 구조로 데이터가 삽입되었으므로, 기존 쿼리 로직을 그대로 활용할 수 있습니다.

| 버전 코드 | 정식 명칭 | 언어 | 절수 | 상태 |
|-----------|-----------|------|------|------|
| `nkrv` | 개역개정 | 한국어 | 31,101 | 기존 |
| `rnksv` | 새번역 | 한국어 | 31,075 | 기존 |
| `kjv` | King James Version | 영어 | 30,966 | 기존 |
| **`easy`** | **통독성경** | **한국어** | **31,100** | **신규** |
| **`nirv`** | **New International Reader's Version** | **영어** | **31,058** | **신규** |
| **`gnt`** | **Good News Translation** | **영어** | **30,170** | **신규** |

**총 데이터**: 6개 버전, 185,470절

---

## 2. 데이터 구조 (변경 없음)

```sql
CREATE TABLE bible_verses (
  id         serial PRIMARY KEY,
  version    varchar NOT NULL,     -- 'nkrv' | 'rnksv' | 'kjv' | 'easy' | 'nirv' | 'gnt'
  book_code  varchar NOT NULL,     -- 'gen' ~ 'rev' (66권 동일 코드)
  book_name  varchar NOT NULL,     -- 한글 또는 영문 책명
  book_abbr  varchar NOT NULL,     -- 약어 (최대 5자)
  book_order smallint NOT NULL,    -- 1~66
  testament  varchar NOT NULL,     -- 'old' | 'new'
  chapter    smallint NOT NULL,
  verse      smallint NOT NULL,
  text       text NOT NULL DEFAULT '',

  UNIQUE (version, book_code, chapter, verse)
);
```

---

## 3. 새 버전별 특성

### 3.1 통독성경 (`easy`)

- **용도**: 한국어 쉬운 표현 성경 (어린이/청소년/초신자 대상)
- **book_name**: 한글 책명 (기존 `nkrv`와 동일: "창세기", "출애굽기" 등)
- **book_abbr**: 한글 약어 (기존 `nkrv`와 동일: "창", "출" 등)


### 3.2 NIrV (`nirv`)

- **용도**: 영어 쉬운 표현 성경 (NIV의 쉬운 버전, 초등학생 수준 영어)
- **book_name**: 영문 책명 (기존 `kjv`와 동일: "Genesis", "Exodus" 등)
- **book_abbr**: 영문 약어 (기존 `kjv`와 동일: "Gen", "Exod" 등)
- **소스**: biblegateway.com
- **참고**: 절 텍스트에 소제목(heading)이 포함된 경우 있음 (예: "The Beginning In the beginning...")

### 3.3 GNT (`gnt`)

- **용도**: Good News Translation (현대 영어, 평이한 표현)
- **book_name**: 영문 책명 (기존 `kjv`와 동일)
- **book_abbr**: 영문 약어 (기존 `kjv`와 동일)
- **소스**: biblegateway.com
- **참고**: NIrV와 동일하게 소제목 포함 가능

---

## 4. 쿼리 예시

### 4.1 버전 목록 조회

```typescript
// 사용 가능한 버전 목록 (하드코딩 또는 DB 조회)
const VERSIONS = [
  { code: 'nkrv',  name: '개역개정',  lang: 'ko' },
  { code: 'rnksv', name: '새번역',    lang: 'ko' },
  { code: 'easy',  name: '통독성경',  lang: 'ko' },
  { code: 'kjv',   name: 'KJV',       lang: 'en' },
  { code: 'nirv',  name: 'NIrV',      lang: 'en' },
  { code: 'gnt',   name: 'GNT',       lang: 'en' },
];
```

### 4.2 장절 검색 (기존과 동일)

```typescript
const { data } = await supabase
  .from('bible_verses')
  .select('*')
  .eq('version', 'easy')  // 또는 'nirv', 'gnt'
  .eq('book_code', 'psa')
  .eq('chapter', 23);
```

### 4.3 한/영 병기 (새 버전 조합)

```typescript
// 통독성경 + NIrV 조합
const [kr, en] = await Promise.all([
  supabase.from('bible_verses').select('*')
    .eq('version', 'easy').eq('book_code', code).eq('chapter', ch).eq('verse', vs).single(),
  supabase.from('bible_verses').select('*')
    .eq('version', 'nirv').eq('book_code', code).eq('chapter', ch).eq('verse', vs).single(),
]);
```

### 4.4 키워드 검색

```typescript
// 영문 키워드 검색 (NIrV)
const { data } = await supabase
  .from('bible_verses')
  .select('*')
  .eq('version', 'nirv')
  .ilike('text', '%love%')
  .order('book_order')
  .limit(50);
```

### 4.5 다중 버전 동시 조회

```typescript
// 같은 구절을 여러 버전으로 동시 조회
const { data } = await supabase
  .from('bible_verses')
  .select('version, text')
  .in('version', ['nkrv', 'easy', 'nirv', 'gnt'])
  .eq('book_code', 'jhn')
  .eq('chapter', 3)
  .eq('verse', 16);
```

---

## 5. UI 확장 제안

### 5.1 버전 선택 UI 변경

기존 "개역개정 ↔ 새번역" 토글을 **드롭다운 또는 탭**으로 변경:

```
┌─────────────────────────────────────┐
│  한국어: [개역개정 ▼] [새번역] [통독성경]  │
│  영  어: [KJV ▼] [NIrV] [GNT]          │
└─────────────────────────────────────┘
```

### 5.2 카드 영문 버전 선택

기존 KJV 고정 → 사용자가 영문 버전 선택 가능:

```typescript
// CardCustomizer에 영문 버전 선택 추가
interface CardConfig {
  // 기존 필드...
  krVersion: 'nkrv' | 'rnksv' | 'easy';  // 한글 버전 선택
  enVersion: 'kjv' | 'nirv' | 'gnt';      // 영문 버전 선택
}
```

### 5.3 book_code 매핑 (변경 없음)

모든 버전이 동일한 `book_code`를 사용하므로 기존 `lib/books.ts`의 66권 목록을 그대로 활용할 수 있습니다.

---

## 6. 주의사항

| 항목 | 내용 |
|------|------|
| book_abbr 길이 | `varchar(5)` 제한. 데살로니가전/후서는 `1Thes`/`2Thes`로 저장됨 |
| 소제목 포함 | NIrV/GNT 일부 절에 소제목이 본문 앞에 포함됨 (예: "The Beginning In the beginning...") |
| RLS 정책 | 기존과 동일 — SELECT only (public). 새 버전도 자동 적용됨 |
| 인덱스 | 기존 인덱스가 새 버전에도 자동 적용됨 (UNIQUE 제약 포함) |
| text 인코딩 | UTF-8, 특수문자 없음 |

---

## 7. 데이터 검증 쿼리

```sql
-- 버전별 절수 확인
SELECT version, COUNT(*) as verse_count
FROM bible_verses
GROUP BY version
ORDER BY version;

-- 66권 완전성 확인
SELECT version, COUNT(DISTINCT book_code) as book_count
FROM bible_verses
GROUP BY version;

-- 특정 버전의 책 목록
SELECT DISTINCT book_code, book_name, book_order
FROM bible_verses
WHERE version = 'nirv'
ORDER BY book_order;
```

---

## 변경 이력

| 일자 | 내용 |
|------|------|
| 2026-05-05 | 통독성경(easy), NIrV(nirv), GNT(gnt) 3개 버전 추가 완료 |

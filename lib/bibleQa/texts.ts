/**
 * 성경 질문 — 앱이 고정으로 쓰는 문구
 *
 * 모델에게 맡기면 빼먹거나 매번 달라진다(docs/BIBLE_QA_DOCTRINE.md §B-2·§B-3).
 * 문구는 지휘부가 정한 그대로다 — 고칠 때는 문서와 함께 고친다.
 */

/** §B-2 면책. 세 답 위에 한 번만 보인다(세 번 반복하면 지저분하다). */
export const DISCLAIMER =
  "AI 가 성경을 찾아 답한 것입니다. 교회의 공식 가르침이 아니며, 중요한 일은 목회자와 나누십시오.";

/** §B-3 여러 답이 갈릴 때. 이제 늘 두 칸 이상이 답한다(§B-3-1) — '세 답' 이 아니라 '답'. */
export const DIVERGENCE_NOTE = "답이 갈리는 대목이 바로 목회자와 나눌 자리입니다.";

/** §B-3-1 교리가 걸려 Claude 가 더해졌을 때 — 칸이 왜 셋인지 알린다. */
export const EXTENDED_NOTE = "교리가 걸린 질문이라 세 AI 가 함께 답합니다.";

/** §B-7 주의 한 줄(지휘부 2026-09-19). 입력창 아래, 외부 전송 고지 위 — 묻기 전에 읽게 한다. */
export const CAUTION_NOTICE =
  "AI 답변은 참고용입니다. 민감하거나 미심쩍은 내용은 꼭 목회자님께 확인하세요.";

/** §B-7 외부 전송 고지. 입력창 아래 한 줄. */
export const EXTERNAL_NOTICE =
  "적으신 내용은 답을 만들기 위해 외부 AI 로 전송됩니다. 이름·연락처나 남의 이야기는 적지 마십시오.";

/**
 * §7 거절 문구. 게이트가 `deny` 면 **모델을 부르지 않고** 이것을 그대로 돌려준다.
 * 값싼 모델에 거절문을 쓰게 하면 말투가 매번 달라진다(§A).
 *
 * 게이트는 갈래를 알려 주지 않으므로 기본은 첫 문구다. 나머지 다섯은 화면·문서에
 * 같은 말이 남아 있어야 해서 함께 둔다(뒤에 갈래를 나눌 때 쓴다).
 */
export const REFUSAL_GENERAL =
  "물어 주신 마음은 알겠습니다. 다만 이 창은 성경을 읽다가 생긴 질문에 답하는 자리라, 그 질문은 여기서 답하지 못합니다. 성경이나 신앙에 대해 궁금한 것을 물어 주시면 함께 찾아보겠습니다.";

export const REFUSALS = {
  general: REFUSAL_GENERAL,
  fortune:
    "그만큼 앞이 막막하신가 봅니다. 앞일은 제가 알 수 없고, 성경도 그것을 알아보려 하지 말라고 말합니다. 대신 지금 무엇을 붙들고 기도할지는 함께 찾아볼 수 있습니다.",
  person:
    "마음이 편치 않으셨겠습니다. 특정한 분에 대한 판단은 제가 할 자리가 아닙니다. 성경이 그 일에 대해 무엇을 말하는지는 말씀드릴 수 있습니다.",
  politics:
    "그 문제로 생각이 많으실 것입니다. 어느 쪽을 지지해야 하는지는 제가 말할 자리가 아닙니다. 성경이 정치와 권세에 대해 무엇을 말하는지는 함께 볼 수 있습니다.",
} as const;

/**
 * §B-1 위기 화면의 머리말. AI 를 부르지 않고 앱이 먼저 띄운다.
 * 창구 목록은 DB(`qa_lists` kind='crisis')에서 오고, 아래는 그것이 비었을 때의 대비다.
 */
export const CRISIS_HEADING = "말씀해 주셔서 고맙습니다. 혼자 두지 않겠습니다.";
export const CRISIS_BODY =
  "지금 여기서 드릴 수 있는 가장 좋은 것은, 바로 닿을 수 있는 곳을 알려 드리는 일입니다. 아래는 24시간 열려 있는 공식 창구입니다.";

/**
 * 창구 목록의 코드 쪽 대비값. **DB 가 비어 있을 때만 쓴다** —
 * 평소에는 관리자 화면에서 고친 DB 값이 먼저다(§B-11).
 * 2026-09-18 공공 안내로 확인했다. **반년마다 한 사람이 다시 확인한다.**
 */
export const CRISIS_FALLBACK: { title: string; body: string }[] = [
  { title: "자살예방 상담", body: "109 · 24시간 365일" },
  { title: "응급 구조·구급", body: "119 · 24시간" },
  { title: "범죄·즉시 위험 · 아동학대 신고", body: "112 · 24시간" },
  { title: "정신건강 위기상담", body: "1577-0199 · 24시간" },
  { title: "여성긴급전화(가정폭력·성폭력)", body: "1366 · 24시간" },
  { title: "청소년 상담", body: "1388 · 24시간" },
  { title: "노인학대 신고", body: "1577-1389 · 가까운 지역 기관으로 연결" },
];

/** 질문 글자 수 상한(§B-10). 넘으면 서버가 400 으로 돌려준다. */
export const QUESTION_MAX_LENGTH = 600;

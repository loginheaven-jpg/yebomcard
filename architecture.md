# architecture.md — 시스템 아키텍처 (현행 + 변경 이력)

> **버전**: 0.7.3
> **최종 갱신**: 2026-09-21
> **상태**: living document — 모든 아키텍처 변경·신규 개발 사항은 본 파일의 **변경 이력** 섹션에 최상단부터 누적 기록한다.

본 문서는 예봄성경 / 예봄카드의 현재 아키텍처를 단일 소스로 기록한다.
시스템 단면별 상세는 [docs/](docs/) 폴더를 참조한다.
보류·차기 검토 사항은 [plan.md](plan.md) 에 누적된다.
역사적 v0.4.0 설계서는 [backup/architecture-v0.4.0.md](backup/architecture-v0.4.0.md) 보관.

---

## 📜 변경 이력 (최신 위)

### 2026-09-23 — 공개 질문이 다른 계정에서 보이지 않는 원인 확인 · 저장 성공 판정 수정
- 운영 DB에 `shared`·`share_hidden_at` 칸이 없었다. 공개 저장이 실패하면 개인 저장으로 재시도해 HTTP 200을 반환했고, 클라이언트가 상태를 확인하지 않아 공개 성공으로 안내했다.
- 공개 요청은 공유 칸이 없으면 503으로 실패한다. 비공개 저장·저장 해제만 기존 호환 처리를 유지한다. 질문 조회 오류·없는 질문·위기/거절도 공개 성공으로 반환하지 않는다.
- 클라이언트는 HTTP 상태뿐 아니라 `success`·`saved`·`shared`가 요청과 일치하는지 확인한다. `/api/bible-qa/health`의 `sharing.ok`는 공유 칸 네 개의 준비 상태를 점검한다.
- DB 적용 파일: `scripts/migration-qa-share.sql`. 기존 개인 저장에는 공개 선택 기록이 없어 일괄 공개하지 않는다. 적용 후 본인이 ‘나의 질문’에서 공개로 다시 설정한다.
- 회귀 확인: `node scripts/verify-qa-sharing.cjs` (스키마 누락·공개/비공개·저장 해제·로그인/소유자·위기/거절·응답 상태 불일치).

### 2026-09-21 — 성경 질문: 함께보기(응답 공유) · 나의 질문
- **왜**(지휘부): 앱은 한 번만 답한다. 그래서 같은 절을 두고 비슷한 궁금증이 생긴 교인은 각자 새로 물어야 했고,
  그때마다 게이트웨이 호출이 한 번씩 더 나갔다. 저장한 질문을 **이름 없이** 서로 보이게 하면 중복 질문이 줄고,
  한 절에 쌓인 질문이 그 절의 주석이 된다. 개인 신앙상담이 아니라 구절 자체에 대한 질문이라
  **공유 가치가 보안 가치보다 높다**는 판단(§B-14)
- **저장 줄에 '비공개' 체크박스. 기본은 공개.** 체크를 못 보고 저장만 누르는 분이 있어
  **저장 뒤 토스트로 무엇이 일어났는지 다시 말한다** — `다른 교인도 이 질문을 볼 수 있습니다(이름은 안 보입니다)`
- **누가 물었는지는 서버가 내려보내지 않는다.** 화면에서 안 그리는 것으로는 모자라 select 에서 `user_id`·`user_name` 을 뺐다.
  **로그인한 교인만** 본다(비로그인 조회에는 함께보기가 없다)
- **위기·거절은 공유하지 않는다** — 화면에 저장 단추가 없고, 서버가 `is_crisis`·`gate_result` 로 한 번 더 막는다
- **익명은 익명이 아니다**(교회 규모에서 구절+내용이 사람을 가리킨다). 그래서 이름을 지우는 데서 끝내지 않고
  ① **신고를 공개된 질문의 답까지** 넓혔다(전에는 자기 질문의 답에만) ② 수퍼어드민이 **내릴 수 있다**
  (`share_hidden_at` — 행은 지우지 않는다). 앱은 한 번만 답하므로 잘못 나간 답을 고칠 길이 없다
- **보이는 자리는 질문 창 안**(묻기 전에 보여야 중복을 막는다). 본문 아래 접힌 줄에는 **내 질문만** 둔다 —
  인기 절에는 공개 질문이 수십 개라 말씀보다 길어진다. 한 장에서 서버 20개 · 창에서 5개
- **설정 → 나의 질문**(`/my-questions`): 내가 한 **모든** 질문을 최근 것부터(저장 안 한 것도). 펼쳐 답 다시 읽기 ·
  공개 ↔ 비공개 바꾸기 · 내 절에서 내리기(내리면 공유도 함께 내려간다). **본인 것만** 보인다 —
  모든 교인의 질문과 이름은 `/admin/ai-questions`(수퍼어드민)에 있고 그 화면은 **열어 본 사실을 남긴다**(§B-10).
  합치면 그 기록을 빠뜨리기 쉬워 일부러 나눴다
- **표가 없어도 선다**: 공유 칸이 없으면 저장만 되고(공유 없이), 관리자 목록은 그 두 칸을 뺀 select 로 다시 읽는다
- **확인**: 가짜 세션·API 로 몰아 봤다 — 묻기 전 '다른 교인의 질문 2개(이름은 보이지 않습니다)' · 펼치면 남의 답 ·
  기본 저장이 `{saved:true, shared:true}` · 비공개 체크면 `{shared:false}` · 토스트 두 갈래 ·
  나의 질문 화면의 뱃지(함께보기·말로·저장 안 함)와 공유 끄기·내리기 요청 본문
- **DDL 은 따로 적용해야 한다** — `scripts/migration-qa-share.sql`

### 2026-09-21 — 성경 질문: 질문을 누르면 선택이 풀린다 · 말로 묻기는 한 번만 누른다
- **선택 해제가 안 되던 까닭**: 질문 창이 `selectedVerses` 를 그대로 보고 있어서, 선택을 푸는 순간
  창이 함께 닫혔다(조건이 `qaOpen && selectedVerses.length > 0`). 그래서 해제를 넣을 수가 없었고,
  창을 닫고 나면 절이 그대로 골라져 있었다. 이제 누를 때 **한 벌 떠서**(`qaVerses`) 창에 넘기고 선택은 바로 푼다
- **말로 묻기 — 세 번 누르던 것을 한 번으로**(지휘부 2026-09-21, §B-13):
  - **말이 끊기면 3.5초에 스스로 마감**한다. 마지막 2초는 `곧 마칩니다 3·2·1` 을 보여 줘,
    더 말씀하실 분이 말을 이어 시계를 되돌릴 수 있게 했다. '듣기 마감'('다 말했습니다' 에서 이름을 바꿨다)은 그대로 둔다
  - **들은 글을 입력창에 보인 채 3초를 센 뒤 스스로 묻는다.** `잠깐, 고칠게요` 를 누르거나 입력창을 건드리면 멈춘다.
    §B-7 '보고 누른다' 에서 **보는 것은 그대로 두고 누르는 것만 셈이 대신한다** — 글을 안 보여 준 채 보내는 것은 금지
  - **소리 문턱을 고정하지 않는다**: 시작 0.6초로 바닥 소음을 재서 그 위로 잡고, 그마저 시끄러우면 자동 마감을 **끈다**
    (고정 문턱이면 거실·교회 소음에 영영 안 끝나 90초 상한까지 간다). **말을 들은 뒤부터** 침묵을 세고
    (마이크를 켜고 생각하는 시간을 침묵으로 세지 않는다), 한 마디도 못 들은 채 12초면 끝내고 게이트웨이로 보내지 않는다.
    소리를 못 재는 기기·시끄러운 곳에서는 `watchingRef` 가 꺼져 **무음 종료가 작동하지 않는다**(말하는 중에 끊기지 않게)
  - **아이폰에서 조용히 실패하던 것**: 녹음 파일 이름을 `question.webm` 으로 박고 있었는데 사파리는 **mp4** 로 녹음한다.
    whisper 는 확장자로 포맷을 가르므로 실제 `mimeType` 에서 뽑아 붙인다
- **확인**: 합성 마이크(AudioContext 발진기 → MediaStreamDestination)로 `getUserMedia` 를 갈아 끼워 실제로 몰아 봤다 —
  말하는 동안 셈이 안 뜨고, 멈추면 `곧 마칩니다` 가 뜨고, 마감 → STT → 입력창 → `3초 뒤에 묻습니다` → **자동 제출**
  (`input_kind: voice`)까지. `잠깐, 고칠게요` 를 누르면 나가지 않고 글은 남으며, 한 마디도 안 하면 12초에 끝난다.
  절 두 개를 골라 질문을 누르면 창 머리가 `요한복음 3:1-2` 이고 본문 선택은 풀린다

### 2026-09-20 — 말씀의삶: 그룹마다 다른 진도표 (만들기 · 고르기 · 서로 보기)
- **진도표가 하나에서 여럿으로**(지휘부). 교회 진도표는 그대로 파일에 남고 이름만 **'표준진도표'** 가 됐다.
  그 밖의 진도표는 **DB**(`reading_plans`)에 있고 만든 사람이 고칠 수 있다. 모든 교인이 만들 수 있다(1인 20개).
  그룹은 하나의 진도표를 읽고, 그룹을 만들 때 고르거나 나중에 **만든 사람이 바꾼다**
- **틀은 서로 보인다**(지휘부): 목록에 남의 진도표도 나오고 **복사해서 내 것으로** 가져갈 수 있다.
  보이는 것은 이름 · 만든 이 · 회차 수뿐이고, **진도(누가 어디까지)는 그룹 안에서만** 보인다. '나만 쓰기' 를 고르면 목록에서 빠진다
- **읽기 시작한 진도표는 잠근다**: 만든 사람 말고 **다른 교인이 한 번이라도 회차를 체크**했으면 범위를 고칠 수 없고
  이름·설명만 고쳐진다(`lockIfOthersRead`). 회차를 바꾸려면 복사해서 새로 만든다 — 남이 읽는 중에 회차가 움직이면 진도가 거짓이 된다
- **지우기 두 단계**: 만든 사람·운영자는 지울 수 있다. **쓰는 그룹이 있으면 목록에서만 내려가고**(`hidden_at`) 그 그룹은 계속 읽는다.
  부적절해서 끝내 지워야 하면 **수퍼어드민만** `?force=1` — 그때 쓰던 그룹의 진도표는 **해제**(`plan_id → null`)되고
  화면이 "진도표가 해제되었습니다 — 다시 고르기" 를 띄운다. **임의로 다른 진도표로 옮기지 않는다**(지휘부)
- **만들기 = 자동 + 손질 + AI**(한 화면): 범위(전체·구약·신약·책 고르기)와 회차 수(또는 한 회 절 수)를 주면
  **절 수가 고르게** 장 경계에서 나눈다(`lib/plans/builder.ts` `autoUnits`, 절 수 표 `lib/plans/verseCounts.ts` — 새번역 실측 31,075절).
  나눈 뒤 줄마다 `창세기 1-3` 처럼 **글로 고치고**(`parsePlanLine`) 나누기·합치기·빼기·더하기를 한다.
  AI(`gemini-flash`)는 "요한복음을 30일에, 주 5일만" 같은 한 줄을 받아 **초안만** 만들고 저장은 사람이 누른다
- **회차 수를 코드에 박지 않는다**: `TOTAL = plan.units.length`, 회차는 배열 인덱스가 아니라 **`seq` 로 찾는다**(`unitBySeq`).
  체크(`reading_unit_checks`)·완료 시트 기억(localStorage)·그룹 순위 캐시가 모두 **진도표별**로 갈라졌다.
  진도표 객체는 **id 마다 하나**로 돌려쓴다(`rememberPlan`) — 엔진의 파생 캐시가 WeakMap 이라 객체가 새로 생기면 매번 다시 센다
- **믿을 수 없는 값은 들어오는 자리에서 거른다**(`sanitizeUnits`): 없는 책 · 장 수를 넘는 범위 · 거꾸로 된 범위 · 긴 이름.
  DB 도 AI 도 같은 문을 지난다
- **표가 없어도 앱은 선다**: 마이그레이션 전이면 목록이 `ready:false` 로 돌아오고 표준진도표만 쓴다(`isMissingTable`)
- **확인**: `npm run verify:planbuilder`(범위 · 자동 분배 365회차까지 · 줄 읽기 · **표준진도표 91회차 글 왕복** ·
  손질 · 검사 · 믿을 수 없는 값 · 엔진 결합) 전부 통과. 왕복 시험이 실제 결함 둘을 잡았다 —
  `열왕기하 11-15:1-22` 처럼 **다시 읽을 수 없는 글**을 뱉던 것(→ `열왕기하 11:1-15:22`)과 회차 이름 한도 40자가
  표준진도표 50자 회차를 걸러내던 것(→ 60자). 화면은 가짜 세션·API 로 몰아 봤다(고르기 · 자동 · 손질 · AI · 저장 ·
  그룹 만들 때 고르기 · 그룹 진도표 바꾸기 · 해제 문구). AI 초안은 실제 게이트웨이로 여섯 줄을 재 봤다
  (요한복음 30일·주5일 → 21회차, 1년 통독 → 365회차 1,189장, 오류 0)
- **DDL 은 따로 적용해야 한다** — `scripts/migration-reading-plans.sql` (이 대화에서는 DB 에 닿지 못했다)

### 2026-09-19 — 성경 질문: 네 목록 편집 화면 · 선별이 고른 구절을 본다 · 가정교회 규칙이 빠지던 구조
- **네 목록 편집 화면**(지휘부): 설정 → 관리자 → '성경 질문 목록 편집'(`/admin/qa-lists`, 수퍼어드민).
  이단 목록 · 가정교회 자료 · 위기 상담 창구 · 삶공부 과정을 탭으로 — 더하기 · 고치기 · 끄기/켜기 · 위로/아래로 · 지우기,
  목록마다 AI 에게 가는 칸과 사람만 보는 '근거 · 메모' 칸을 나누고 쓰임을 적어 두었다. API `/api/admin/qa-lists`
  (GET · POST · PATCH(칸 · `enabled` · `move`) · DELETE)는 질문 기록과 같은 문(`requireFreshAdmin` + `isSuperAdmin`),
  고친 사람과 시각을 `updated_by` · `updated_at` 에 남긴다. 고친 것은 다음 질문부터 바로 쓰인다
- **가정교회 규칙이 빠지던 구조**: DB 에 가정교회 자료가 한 줄이라도 있으면 문서 블록을 통째로 대신해 '이 블록을 쓰는 규칙'
  (다른 교회 소그룹과 견주지 않기, 교회 살림은 모른다고 하기 …)까지 빠졌다. 이제 **DB 자료 + 문서의 규칙 절**을 합친다.
  문서의 자료 14줄은 DB 로 옮겨 화면에서 고친다
- **선별이 고른 구절을 본다**(지휘부 승인): 선별 모델이 질문 글만 받아 '무슨뜻인가?' 를 거절하던 것(운영 기록,
  요한복음 14:20)을 고쳤다 — `고른 본문: 요한복음 14:20` 한 줄을 붙이고 §A 에 규칙 두 줄. 본문 글은 붙이지 않는다
  (욥 3:11 '무슨 뜻인가요?' 를 위기로 오판). 실제 앱 코드로 구절 질문 40/40 · 기존 선별 42/42(위기 18/18) · 가르기 46/48
- **이단 목록**: 말씀보존학회(이송오) · 세광중앙교회(김노아·김풍일) 추가(합신 결의는 확인 못 함 — 지휘부 결정)
- **확인**: 가짜 세션 · 가짜 API 로 화면을 조작해 보니 수퍼어드민 아니면 막힘, 고치기 · 순서 · 켜기 · 더하기 · 지우기(확인창)가
  맞는 요청을 보냄, 삶공부 탭엔 설명 칸이 없음, 폰 폭에서 가로 넘침 없음

### 2026-09-19 — 성경 질문: AI 고르기를 없애고 서버가 칸을 정한다 · 주의 한 줄 · 선별 gemini-lite
- **칸은 앱이 정한다**(지휘부): '묻기' 하나만 두고, 선별이 `basic` 이면 Gemini · ChatGPT 두 칸, `doctrine`
  (또는 선별 실패)이면 Claude 까지 세 칸. 예전의 AI 단추 넷(Gemini · ChatGPT · Claude · 합창)과 '마지막 고른 AI 기억'
  (`yebom_qa_ai`)을 없앴다. 1단계 라우트가 칸을 정해 `columns` · `extended_note` 로 돌려주고, 요청에 실린 `columns` 는 따르지 않는다.
  PC 격자는 칸 수에 맞춰 둘 · 셋으로 나눈다(두 칸을 세 칸 격자에 두면 오른쪽 1/3 이 빈다)
- **근거**(가린 채점, 대결과 무관한 심사자 셋): 세 칸끼리는 교리 질문에서도 비슷했다(자기 회사 심사자를 빼고
  Claude 8.25 · Gemini 8.43 · ChatGPT 8.00). Claude 는 한 답 약 $0.04 라 교리 질문에만 부른다.
  Mistral Large 3 · DeepSeek 도 쟀으나 넣지 않았다(`plan.md`)
- **선별 한 번으로 칸까지**: 교리 기준 §A 프롬프트의 `allow` 를 `basic` / `doctrine` 로 나눴다. 보기에 없는 문장으로
  42/42(위기 18/18) · 가르기 48/48. 선별 모델은 게이트웨이 `gemini-lite` 별칭(모델 이름을 앱에 박지 않는다, 0.9초).
  기록의 `gate_result` 는 옛 값(`allow`)을 쓰고 낱말은 `gate_raw` 에 — 관리자 화면 · 옛 기록과 같은 값
- **주의 한 줄**(지휘부): 입력창 아래 `AI 답변은 참고용입니다. 민감하거나 미심쩍은 내용은 꼭 목회자님께 확인하세요.`
  (§B-7, 외부 전송 고지 위). '세 답이 갈리는 대목' 은 '답이 갈리는 대목' 으로
- **확인**: 실제 선별 코드로 8문장(두 칸 3 · 세 칸 3 · 위기 · 거절) 모두 맞음. 가짜 응답으로 그려 보니 옛 AI 단추 0개 ·
  '묻기' 1개 · 주의 문구, 교리 질문은 세 칸 격자와 안내 두 줄, 용어 질문은 두 칸 격자, 요청에 `columns` 없음

### 2026-09-18 — 성경 질문: 이어가기 안내 · 저장한 질문을 창에서 · 구절 셋 · 게이트웨이 오류 문구
- **이어가기 안내**(지휘부 — "Gemini 는 조용히 빈 대화창으로 인도한다"): 복사한 뒤 **떠나기 전에** 안내를 띄우고,
  창은 안내의 '열기' 에서 연다. 세 AI 모두 — Gemini 는 "미리 채워 줄 수 없으니 붙여넣기", ChatGPT·Claude 는
  "채워져 있고, 비어 있으면 붙여넣기"(글이 길면 앞부분만 채운다고 알림), 폰은 '길게 눌러', PC 는 'Ctrl+V',
  복사가 막히면 글을 직접 보여 준다, 로그인이 필요하다는 한 줄. 예전에는 창을 먼저 열고 안내를 떠나온 창에 띄워
  보이지 않았고, `await` 뒤의 `window.open` 은 아이폰이 팝업으로 막을 수 있었다
- **저장한 질문을 창에서**(지휘부 — "저장된 구절에서 질문을 누르면 기존 응답을 물고 들어가야"): 묻기 전 화면 맨 위에
  "이 절에 저장한 질문 N개" 를 접어서 보인다. 고른 절 범위와 겹치는 것만, 창이 스스로 읽는다(검색 화면에서 열어도 보이게).
  앱은 한 번만 답하므로 여기서 이어 묻지 않고, 다시 읽거나 그 AI 로 이어가거나 신고한다
- **구절 셋**: 없는 구절 지우기(§B-5)는 전부 검사하고, 본문을 붙이는 것(§B-9)은 앞의 셋까지만 — 비교 측정에서
  ChatGPT 가 한 답에 6개를 달아 카드가 답보다 길어졌다(교리 기준 §4 는 셋까지)
- **게이트웨이 오류 문구**(`lib/aiGateway.ts`): 게이트웨이는 끝점마다 오류 꼴이 달라, `${err.detail}` 만 찍으면
  폴백 전부 실패(503 객체)는 `[object Object]`, 이미지 생성(`{error, code}`)은 `undefined` 가 됐다. 꼴마다 읽게 고쳤다
- **확인**: 가짜 응답으로 그려 보니 저장 질문 1개만(범위 밖은 숨김) 보이고, 이어가기 안내가 먼저 뜨며 '열기' 에서만
  창이 열리고(취소하면 안 열림), 저장한 질문으로 이어가면 그 질문 글이 복사되고, 폰·PC 문구가 갈렸다

### 2026-09-18 — 성경 질문: 마지막 줄을 '더 깊은 묵상' 으로 · PC 창 90%
- **마지막 줄**(지휘부): '목회자와 나눠 볼 질문' → **'더 깊은 묵상'**. 지시문도 "교인이 스스로 묵상하거나
  **목장에서 나눌 수 있는** 물음, '나' 를 주어로" 로 바꿨다(교리 기준 §4 · 보기). 목회자에게 이어 주는 일은
  앱의 고정 문구(§B-2 면책 · §B-3 합창 한 줄)가 그대로 맡는다. 프롬프트 판 **v2**.
  이미 저장된 답은 옛 제목이라 파서가 **두 제목을 모두** 읽는다(옛 글자를 기준으로 본문을 자른다 —
  새 제목으로 자르면 옛 답의 마지막 줄이 빈다). 세 모델 모두 새 제목·묵상 물음으로 답하는 것을 실제 호출로 확인
- **PC 창 90%**(지휘부 — "PC 풀스크린에서도 좌우 여백이 많다"): 가로·세로 90%. 합창은 넓어진 폭을 세 칸이
  다 쓰고, 하나만 묻거나 입력 중일 때는 읽기 좋은 폭(최대 896px)으로 가운데 둔다 — 90% 폭에서 한 줄이
  150자를 넘으면 눈이 줄을 놓친다. 세 칸 나란히는 **1024px 이상에서만**(태블릿 폭에서는 쌓는다). 폰은 그대로 80%

### 2026-09-18 — 성경 질문: 먼저 끝난 답부터 · 운영에서 꺼져 있던 위기 선별
- **계기**: 지휘부가 "답이 나왔는데 상당히 오래 걸린다. 셋 다 동시에 보여 주기보다 완료된 것 먼저 차례로
  보여 주면 어떨까" 라고 했다. 운영 기록을 재 보니 느린 것보다 더 큰 문제가 있었다
- **운영 실측**(첫 두 질문, 둘 다 합창): **선별이 둘 다 시간 초과로 '통과'**(`gate_result=error`) —
  위기 선별이 사실상 꺼져 있었다. **Claude 는 둘 다 40초에 잘렸다**(게이트웨이가 잰 모델 시간만 31~36초,
  입력 16.8K 토큰). Gemini·ChatGPT 도 우리 쪽에서 30~37초
- **원인은 게이트웨이 앞단**: `/health` 는 0.4~0.8초인데, 모델을 부르지 않는 **캐시 적중이 3.9~30.8초**,
  한 낱말 선별 벽시계가 10~14초, 셋을 동시에 보내면 각각 약 2배로 느려진다. 앞서 8초 제한을 잡을 때
  이 PC 의 네트워크 탓으로 본 몫이 실은 게이트웨이 탓이었다. 게이트웨이는 이 저장소 밖이다
- **두 단계로 나눴다**: `/api/bible-qa` 는 선별·기록만(위기·거절 판정), `/api/bible-qa/answer` 를
  **칸마다 따로** 부른다. 화면은 **도착한 순서대로** 카드를 놓고, 아직 안 온 칸은 '찾고 있습니다… N초' 로 둔다.
  합창에서 처음 온 답은 펼치고 뒤에 온 답은 접는다(§B-8 — 나머지를 기다리는 동안 읽을 것이 있어야 한다).
  칸 하나가 라우트 상한(120초)을 통째로 쓰니 Claude 가 잘리지 않는다(칸 기다림 100초)
- **선별 20초 + 안전망**(`lib/bibleQa/crisisSignal.ts`): 선별이 실패했을 때 1인칭 위기 말이 있으면
  통과가 아니라 위기로 본다. 선별이 제때 답하면 선별을 따른다. `npm run verify:crisis` 26항목
- **새 열**(Supabase 적용, 모두 nullable): `ai_questions.columns`(1단계가 고른 칸 — 2단계가 그 칸에만 답한다) ·
  `gate_ms` · `gate_raw`(선별이 왜 실패했는지) · `local_signal`
- **검수**(다른 대화가 한 일): 표 적용과 'AI 단추가 곧 묻기' 는 맞다. 다만 `tsconfig.verify.json` 의
  스크립트 목록이 빠져 `npm run ingest:sermons` 가 컴파일되지 않던 것(내 앞 커밋의 실수)을 함께 고쳤다
- **확인**: 가짜 응답을 칸마다 1.2·3·6초로 늦춰 그려 보니 ChatGPT → Gemini → Claude 순서로 뜨고,
  위기면 답 요청이 하나도 나가지 않았다. **운영에서 실제 걸리는 시간은 게이트웨이가 정한다** — 선별 10~20초 뒤
  첫 답이 온다. 게이트웨이를 빠르게 하는 것은 이 저장소 밖의 일이다

### 2026-09-18 — 성경 질문: 표 적용 · AI 단추가 곧 '묻기'
- **계기**: 지휘부가 질문하면 `Could not find the table 'public.ai_questions' in the schema cache` 가 난다고 알렸다.
  배포는 끝났는데 `scripts/migration-bible-qa.sql` 이 **적용 대기**인 채였다(`/api/bible-qa/health` 의 표 7개 모두 false)
- **표 적용**: 같은 파일을 Supabase 에 적용(`bible_qa_questions_lists_sermons`) + `notify pgrst, 'reload schema'`.
  표 7개 모두 RLS 켬·정책 0 을 쿼리로 확인, 이단 10건·위기 창구 7건이 들어갔다. 점검 창구 `ok: true`
- **AI 고르기 → 묻기 한 번으로**(지휘부 제안, `components/BibleQaSheet.tsx`): 위의 AI 고르기 줄과 아래 '묻기' 를
  합쳐 입력창 **바로 아래** [Gemini][ChatGPT][Claude][합창] 을 두고, 누르면 그 AI 에게 바로 묻는다.
  마지막에 누른 것이 채워진 단추로 보인다(기억은 그대로 `yebom_qa_ai`). 창 맨 아래에 붙이지 않은 것은
  폰 키보드에 가리기 때문이다. 360·390px 폭에서 네 단추가 한 줄에 드는 것을 화면으로 확인
- **남은 사람 손**: 드라이브 설교 폴더를 서비스 계정에 공유 · Vercel 에 `CRON_SECRET`
  (없으면 설교 자동 들여오기가 막힌다 — 점검 창구가 알린다)

### 2026-09-18 — 성경 질문 완성: 질문창 · 저장 · 기록 화면 · 음성 · 설교 카드
- **질문창**(`components/BibleQaSheet.tsx`) — 80% 시트(PC resize), AI 고르기(마지막 값 기억·처음 Gemini),
  세 칸 답(폰 세로 · PC 3분할), 면책 한 줄·갈리는 대목 한 줄·답마다 신고, 다른 AI 로 이어가기
  (ChatGPT·Claude 는 주소로, Gemini 는 클립보드). 절 선택 카드에 '질문'(로그인 전용)
- **구절 검증**(§B-5·§B-9, `lib/bibleQa/verseRefs.ts`) — 답에 적힌 주소를 `bible_verses` 로 찾아보고
  없는 것은 **표시만** 지운다(글을 다시 쓰지 않는다). 기록에는 모델이 준 원문과 지운 주소를 함께 남겨
  "어느 모델이 구절을 지어내는가" 를 볼 수 있게 했다
- **교인이 저장한 질문** — 절 아래 접힌 ❓ 줄. 지우면 그 절에서 내려가고 **관리자 기록은 남는다**
- **수퍼어드민 기록 화면**(`/admin/ai-questions`) — 위기·신고 걸러 보기, 위기 검토 표시, 개별 삭제.
  목록·검토·삭제를 모두 `ai_question_views` 에 남긴다. **admin 등급은 못 본다**(화면으로 확인)
- **말로 묻기** — `hooks/useQaVoiceInput.ts`(이 저장소 첫 MediaRecorder) + `app/api/bible-qa/stt`.
  들은 글은 **바로 보내지 않고 입력창에 이어 붙인다**. 90초에서 스스로 멈추고, 어느 길로 끝나든
  마이크 트랙을 놓는다. 가짜 마이크로 실제 녹음까지 돌려 확인
- **설교 카드** — `lib/sermons/{parse,ingest}.ts` · `/api/cron/ingest-sermons`(첫 `vercel.json`,
  매일 20:00 UTC) · `/api/bible-qa/sermons`(**답변과 다른 라우트**, 1시간 캐시).
  파서가 2025·2026 두 머리글 꼴을 모두 읽는다(키 이름이 해마다 달랐다) — `npm run verify:sermon` 26항목.
  카드 조회는 질문과 **같은 순간**에 나가고, 위기·거절이면 버린다
- **구글 토큰 공용화** — `lib/google/accessToken.ts`. TTS 가 쓰는 `GCP_SERVICE_ACCOUNT_JSON` 을
  scope 만 `drive.readonly` 로 바꿔 쓴다 → **새 비밀값 없음**. 리팩터 뒤 TTS 를 실제로 합성해 확인
- **프로덕션 점검 창구**(`/api/bible-qa/health`) — 배포 번들에 교리 문서가 실렸는지, 표가 준비됐는지 본다.
  이 창구가 **제 실수를 먼저 잡았다**: supabase-js `head: true` 는 없는 표를 '있다' 고 답한다
  (HEAD 응답에 본문이 없어 404 를 에러 객체로 만들지 못한다) → `.select("id").limit(1)` 로 고쳤다
- **남은 것은 사람 손 둘** — ① `scripts/migration-bible-qa.sql` 실행 ② 드라이브 폴더를 서비스 계정에
  보기 권한 공유. 프로덕션 응답 시간은 배포 뒤 한 번 재야 한다

### 2026-09-18 — 성경 질문: 서버 쪽 한 벌 (선별 · 병렬 답변 · 기록). 표는 적용 대기
- **계기**: 교리 기준·화면 시안·설교 활용까지 지휘부 재가가 끝나 코드 작업에 들어갔다. 이번 몫은 **서버**다 —
  질문창 UI·음성·관리자 화면·설교 카드는 다음 몫
- **새 파일**: `lib/bibleQa/{prompt,gate,columns,texts}.ts` · `app/api/bible-qa/route.ts` ·
  `scripts/migration-bible-qa.sql`(표 7개) · `scripts/verify-bible-qa-prompt.ts`(`npm run verify:qa`)
- **원칙 문서를 저장소에서 읽는다**(§B-11): 교리 본문(§1~§10)과 선별 프롬프트(§A)를
  `docs/BIBLE_QA_DOCTRINE.md` 의 마커·코드펜스에서 잘라 `system_prompt` 로 보낸다.
  `next.config.ts` `outputFileTracingIncludes` 에 문서를 넣었고, 빌드 산출물의 nft 로 실제 포함을 확인했다
  (빠뜨리면 로컬만 되고 배포에서 조용히 깨진다). 자주 바뀌는 네 목록은 `qa_lists` 로 DB 에 두고,
  마이그레이션이 이단 10건·위기 창구 7건을 심는다
- **기록을 먼저 남긴다**(§B-10): 선별 뒤 `ai_questions` INSERT → 그다음에 모델을 부른다. 표가 없으면
  돈을 쓰기 전에 503 으로 멈추고, 답이 늦게 실패해도 기록은 남는다. 위기·거절도 같은 표에 남는다
- **실측으로 드러난 초안의 사실 오류 셋**(재가 문서를 고쳤다)
  1. **선별 `max_tokens: 8` 은 게이트를 완전히 죽인다.** Gemini 는 thinking 토큰이 한도를 먹어,
     15개 질문 **전부**가 `[Empty response: FinishReason.MAX_TOKENS]` 였다(정확도 **0/15**).
     실패는 통과로 처리하니 **위기 신호를 한 번도 못 잡으면서 오류도 안 난다.** `1024` 로 올려 **15/15**,
     출력 토큰은 호출당 1개뿐이라 값은 거의 같다
  2. **선별 타임아웃 1.5초는 짧다.** 게이트웨이가 스스로 잰 시간이 중앙값 1.07초·최대 7.26초 →
     1.5초면 상당수가 선별 없이 통과한다. 8초로 올렸다(놓치는 쪽이 기다리는 쪽보다 나쁘다)
  3. **칸 라벨을 응답 `provider` 로 그리면 안 된다.** 그 필드는 계열명으로 정규화돼
     정상 `gemini-flash` 와 강등된 `gemini-pro→flash` 가 **같은 `"gemini"`** 로 온다.
     라벨의 진실은 `model` 뿐이라 칸마다 기대 접두어(`gemini-`·`gpt-`·`claude-`)를 대조하고,
     어긋나면 라벨을 거짓으로 그리는 대신 칸을 비운다. 폴백은 `use_fallback:false` 로 끈다(동작 확인)
- **`gemini-pro` 는 게이트웨이에서 고장나 있다**(설정 모델 ID 404) → Gemini 칸은 `gemini-flash`.
  고쳐지면 `lib/bibleQa/columns.ts` 한 줄
- **`maxDuration = 60`**: 이 저장소 첫 사용례다. 게이트(≤8초)+세 칸 병렬(칸당 ≤40초)이
  플랫폼 기본 상한을 넘어, 없으면 답이 다 왔는데도 조용히 504 가 된다
- **`isSuperAdmin` 신설**(`lib/admin.ts`): 질문 기록 열람은 super_admin 만(§B-10). `ADMIN_LEVELS` 는
  합집합을 유지해 수퍼어드민이 기존 관리자 기능에서 빠지지 않게 했고, 비상 진입로(`ADMIN_EMAILS`)는
  **합치지 않았다** — 등급 없이 교인의 질문 기록을 볼 길을 만들지 않는다
- **검증**: `npm run verify:qa -- --live` 22항목 통과 + 실제 claude-sonnet 호출이 §4 네 줄 틀·구절 2개·
  면책 문구 없음으로 돌아왔다. `npx tsc --noEmit` 0건, `npx next build` 성공, nft 에 문서 2개 포함 확인
- **남은 것**: 마이그레이션 **적용 대기**(지휘부 실행) · 질문창 UI · 음성(STT) · 다른 AI 로 이어가기 ·
  관리자 기록 화면 · 설교 들여오기와 카드. 프로덕션 응답 시간은 배포 뒤 한 번 재야 한다

### 2026-09-17 — 초대링크가 말씀의삶에 닿지 않던 문제 (카카오톡 → 크롬으로 넘길 때 코드가 빠짐)
- **계기**: 지휘부가 초대링크를 눌러도 말씀의삶 화면에 가지 않는다고 알렸다. 배포 직후 크롬 흉내로는 도착했었다
- **원인**: 카카오톡 등 **안드로이드 인앱 브라우저는 `PwaInstall` 이 크롬으로 다시 연다**(intent). 넘기는 주소로 `location.href` 를 썼는데, 그 effect 는 page 의 effect 보다 **뒤에** 돈다(layout 에서 `{children}` 이 앞) — page 가 `?join=` 을 읽고 주소창에서 지운 뒤라 **크롬에는 코드 없는 `/` 가 열렸다**. 운영에서 카톡 UA 로 열어 확인: `intent://bible.yebom.org/#Intent;…`(코드 없음). 카톡으로 받은 링크는 거의 다 이 길을 탄다. 본문가기(`?goto=`, 공유 페이지)도 같은 구멍이었다
- **고침**: 앱이 처음 열린 주소를 모듈 평가 시점(React effect 이전)에 떠 두고(`lib/launchUrl.ts` `LAUNCH_URL`) 크롬에 그 주소를 넘긴다 → `intent://…/?join=YB7K2M#Intent;…`
- **도착지 = 진도표**(지휘부): 로그인돼 있어 곧바로 참여하거나 로그인하고 돌아와 참여해도 그룹 탭으로 넘기지 않고 진도표에 머문다(알림으로 참여를 알린다)
- **검증**(로컬 빌드): 카톡 UA → 크롬 intent 주소에 코드 포함 · 로그인 상태 링크(최근 읽기 있음/없음) → 진도표 · 비로그인 링크 → 진도표 + 초대 창 · 로그인하고 돌아옴(코드 기록) → 진도표. 로그인 상태는 로컬 전용 세션으로 흉내 내고 그룹 API 는 가짜 응답으로 막아 운영 DB 에 쓰지 않았다

### 2026-09-17 — 메뉴 세 자리의 역할을 나눔 · 성경 탭 · 말씀의삶 탭 복귀 · 초대링크
- **계기**: 지휘부가 하단 탭과 ⋮ 메뉴의 관계를 물었다 — 지금 묶음이 최선인가, ⋮ 로 모두 보내고 탭을 없애야 하나
- **판단(지휘부 승인)**: 탭을 없애지 않는다 — 숨긴 메뉴는 덜 쓰이고(고령 교인), 예배 중 찬송가·본문은 한 번에 눌려야 하며, 본문 몰입은 이미 탭바 자동 숨김이 맡는다. 대신 문제 셋을 고쳤다 ① 교회 전체가 날마다 쓰는 말씀의삶이 9-12 찬송가에 자리를 내준 뒤 ⋮ 안에만 있었다 ② ⋮ 에 '이 화면 보기' · '다른 곳 가기' · '한 번 하는 일' 이 섞였다 ③ 목차 탭과 본문 탭이 겹쳤다(본문 제목 → 장 목록 → ‹ 목차)
- **세 자리의 역할**: 하단 탭 = 자주 가는 곳 · ⋮ = 지금 이 말씀을 어떻게 볼까 · 설정 = 한 번 정해 두는 것 ([docs/IA_5TAB.md](docs/IA_5TAB.md) §0)
  - **하단 탭**: 성경 · 검색 · 말씀의삶 · 찬송가 · 책갈피 · 설정. **성경** = 옛 목차+본문 — 읽는 중에 누르면 목차, 그 밖에서는 마지막 읽던 곳(판정은 page.tsx `isReadingView`). 탭 강조는 화면을 보고 정한다(말씀의삶은 플랜 헤더·완료 시트·초대링크로도 들어온다)
  - **⋮**: 글자 크기 · 한 절씩 크게 · 예배성경 · 앱으로 설치 · 로그인. 로그인은 자주 쓰므로 여기에 둔다(지휘부). 앱으로 설치는 설정의 도구가 없어져 여기에만 남는다
  - **설정**: 찬송가 큰 카드와 '도구'(예배성경 · 성경카드 · 앱으로 설치)를 지웠다(지휘부). 성경카드는 절 선택 → '이 말씀으로 카드 만들기' 로 만든다 — 설정의 성경카드가 열던 `CardBuilder` 는 진입로가 없어져 page.tsx 연결을 뗐다(파일은 남김, [plan.md](plan.md))
- **⋮ 을 누르면 하단 탭바가 함께 올라온다**(지휘부 — 메뉴가 두 무리라는 것을 알게): 메뉴가 열린 동안 자동 숨김 판정에서 빠지고(`shouldAutoHideTabBar` 에 `!moreMenuOpen`) 탭바를 메뉴의 바깥 닫기 막(z-59) 위로 올린다(`BottomTabBar raised`, z-61). 그 사이 탭을 누르면 탭이 먹고 메뉴는 닫힌다(`moreMenuCloseNonce`). 닫으면 다시 3초 뒤 감춘다
- **말씀의삶 초대링크**(지휘부): 코드를 받아 적어 넣지 않고 **링크를 누르고 로그인만 하면 참여**. 코드 입력 경로는 그대로
  - 만들기: 그룹 카드 초대코드 오른쪽 **초대링크** — 폰은 공유 창(카톡 등), 없으면 문구+링크 복사. `{origin}/?join=코드`
  - 받기: page.tsx 가 `join` 을 읽어 URL 에서 곧바로 지우고 말씀의삶을 연다. 로그인돼 있으면 묻지 않고 참여 → 그룹 탭. 아니면 초대 모양의 코드 창('함께 읽는 그룹에 초대받았습니다', 코드 채움, **로그인하고 참여** 하나) — 확인 창 없이 곧장 로그인 화면으로
  - **코드를 받는 순간 기기에 적지 않는다**: 세션 확인이 401 이면 `SessionContext` 가 적어 둔 코드를 지운다(남의 그룹 자동 참여 방지 장치). 메모리에 들고 있다가 로그인하러 떠나기 직전에 적는다. SSO 는 쿼리 없이 홈으로 돌아오므로 이 기록이 유일한 다리다
  - **함께 고침 — 로그인하고 돌아와도 말씀의삶을 다시 찾아 들어가야 참여되던 것**: 참여할 코드가 남아 있으면 page.tsx 가 말씀의삶을 저절로 연다. 진입 때 코드 창에서 코드를 넣고 로그인한 교인도 같은 덕을 본다
- **검증**: 로컬 운영 빌드 390px 모바일 흉내 — 탭 여섯 이름 · 성경(최근 없음 → 목차) · 본문에서 3.5초 뒤 탭바 숨음 → ⋮ 누르니 올라오고 z-61 · 메뉴 연 채 3.5초 그대로 · 메뉴 연 채 성경 탭 → 목차로 가고 메뉴 닫힘 · 목차에서 성경 탭 → 창세기 3장 · 말씀의삶 탭 강조 · ⋮ 메뉴 5항목(말씀의삶 없음) · 설정 시트에 찬송가 카드·도구·앱 설치 없음 · 비로그인 `/?join=yb7k-2m` → URL 이 `/` 로 정리되고 초대 창에 `YB7K2M`, 버튼 '로그인하고 참여'/'나중에'. **로그인 상태의 곧바로 참여와 초대링크 공유 버튼은 SSO 가 필요해 운영에서 확인해야 한다**

### 2026-09-17 — 화면 머리줄 통일: 목차·장 목록·검색·첫 화면도 본문 바와 한 틀
- **계기**: 지휘부가 장 목록 화면을 보고 상단이 통일돼 있지 않고 옛 모양이 남았다고 했다. 9월 12일 개편 때 본문 화면만 새 바로 바꾸고, 나머지 다섯 화면(첫 화면·검색·주제 추천·목차·장 목록)이 쓰는 3칸 줄은 그대로 두었었다
- **옛 줄과 다르던 것**: 칩 30px·모서리 4·글자 11px(본문 40·10·13) · '대역 선택…'(본문 '대역') · 글자 크기 '가' 버튼(본문은 ⋮ 안) · ⇄ 교환은 대역 유무에 따라 26×30 ↔ 42×34 로 **크기가 바뀌었다** · 장 목록은 그 아래 '‹ 목차로 · 민수기' 줄이 하나 더 · 재생 중엔 떠 있는 플레이어가 오른쪽 위를 덮어 **칩과 '가' 를 숨겼다**
- **새 틀**(지휘부 선택): 모든 화면이 `[두 줄 제목] … [40px 조작]` 한 줄. 목록 화면은 `제목 · 역본 · ⋮` — 대역은 뺐다(목록엔 쓸모가 없고 검색은 원래 모든 역본을 함께 보인다). 제목은 첫 화면 Yebom 로고 · 말씀 찾기 / N건 · 주제 추천 / N절 · 성경 목차 / 구약 39권 · `‹ 민수기 / 총 36장`(누르면 목차 — 옛 '목차로' 줄을 합쳐 칸이 한 줄 더 보인다)
- **빠진 것**: ⇄ 교환(역본 칩에서 바로 고르면 된다), '가' 버튼(⋮ 의 글자 크기). ⋮ 은 이제 모든 화면에 같은 메뉴로 있고 '한 절씩 크게' 는 절이 보이는 화면에만 나온다
- **한 벌로 모음**: 역본 칩·대역 칩·⋮ 메뉴·플레이어 줄을 `SearchPanel` 안 조각(`mainVersionChip`·`subVersionChip`·`moreMenu`·`playerRow`)으로 두고 본문 바와 목록 머리줄이 함께 쓴다 — 두 벌로 갈라져 다시 어긋나지 않게
- **같은 자리**: 본문 바는 위의 빈 책갈피 칸(`mb-2`) 밑이라 목록 머리줄보다 8px 아래였다. 목록 머리줄을 `mt-2` 로 내려 화면을 오갈 때 바가 튀지 않는다(모든 화면에서 칩 26–66px 실측)
- **재생 중**(지휘부 선택): 모든 화면에서 머리줄 아래 줄에 인라인 플레이어. 본문은 끝이 ▾ 접기(끝내기는 위 읽기 버튼), **목록 화면에는 읽기 버튼이 없어 ✕ 듣기 종료**를 둔다(`TTSMiniPlayer` — 인라인인데 `onCollapse` 가 없으면 종료)
- **함께 고친 결함 — 말씀의삶에서 플레이어가 사라짐**: `SearchPanel` 은 다른 화면으로 가도 언마운트되지 않고 `display:none` 으로만 가려진다. 본문에서 읽다가 ⋮ → 말씀의삶으로 가면 가려진 인라인 플레이어가 등록된 채라 **떠 있는 플레이어가 물러나 화면에 플레이어가 하나도 없었다**. `isActiveView`(page.tsx 가 `view === "search"` 로 넘김)가 거짓이면 인라인을 그리지 않는다 — 이번 변경으로 인라인이 모든 목록 화면에 붙어 이 틈이 더 넓어질 참이었다
- **검증**: 로컬 운영 빌드를 390px 모바일 흉내로 — 다섯 화면 모두 새 머리줄·옛 ⇄/'가' 없음 · 장 목록 제목 ‹ → 목차 · 목차 화면 ⋮ 메뉴(한 절씩 크게 없음) · 본문 재생 중 인라인+접기 → 장 목록·목차에서 인라인+종료 → 말씀의삶에서 떠 있는 플레이어 → ✕ 로 끝남. **말씀의삶 머리줄은 이번 범위 밖**(지휘부) — [plan.md](plan.md)

### 2026-09-17 — 본문 상단 바 균형: 조작 높이를 맞추고 제목을 키움
- **계기**: 지휘부가 역본 칩과 읽기 아이콘의 위아래가 다르고, 제목과 역본 사이가 넓어 균형이 안 맞는다고 했다
- **실측(390px)**: 역본·대역 칩 36px·모서리 8px, 읽기 44px·모서리 12px — 읽기가 위아래로 4px씩 튀어나왔다. 제목 버튼이 남는 폭을 모두 가져가는데 글자는 56px 뿐이라 칩까지 약 100px(540px 폭에서는 약 250px)이 비었다
- **고침**(지휘부 선택: 배치는 그대로, 제목만 키움): 조작을 모두 **40px·모서리 10px**(칩 폭 70px·글자 11.5→13px, 간격 4→6px). 44px 로 맞추면 칩이 무거워 40 으로 했다. 제목 책 이름 15.5→18px·장 번호 19→24px, 아래 줄은 10.5→12.5px 에 **'총 N장'**('150장' 만으로는 '150장째' 로 읽혔다). 두 무리([제목·역본] … [읽기·⋮])로 나누는 안도 입혀 봤으나 9월 12일의 '조작은 엄지 닿는 오른쪽' 원칙을 지켰다
- **줄 높이는 그대로 44px**(⋮ 칸이 정한다) — 본문 높이 상수 `browseMaxHClass` 를 고치지 않았다. 제목 위아래 여백(py-1)을 빼 두 줄이 41px 안에 들어간다
- **360px 미만 폰**: 그대로 두면 320px 에서 '시편 90' 이 '시·· 90' 으로 잘렸다. 그 폭에서만 칩 64px·간격 4px·제목 16/21px 로 두어 '창세기 50'·'시편 119' 까지 잘리지 않는다(로컬 빌드로 320·390·540px 실측)

### 2026-09-17 — 모바일 장 넘김 스와이프가 잘 안 먹던 문제
- **계기**: 지휘부가 모바일에서 좌우 스와이프로 장이 잘 안 넘어간다고 알렸다. 2026-09-12 에 장 이동 화살표를 뺀 뒤로 스와이프가 모바일의 주된 장 이동이라, 전에는 가려져 있던 서툶이 드러났다
- **원인 넷**(`SearchPanel` `handleVerseSwipe*`): ① 화면 **좌우 20%** 에서 시작한 터치를 버렸다 — 390px 폰에서 각 78px, 오른손 엄지로 '다음 장' 을 밀면 시작점이 거기 걸렸다. iOS 뒤로가기는 가장자리 약 20px 에서만 시작하므로 필요한 것보다 네 배 가까이 넓었다 ② **손을 뗄 때 끝점 각도 30°** 로만 판정해 원을 그리는 엄지 궤적이 떨어졌다 ③ 본문 스크롤 칸에 `touch-action` 이 없어 가로 움직임도 브라우저가 스크롤로 가져갈 수 있었다 ④ 손가락을 따라 움직이는 것이 없어 안 넘어가도 왜인지 알 수 없었다
- **고침**: 가장자리 24px 만 제외 · 10px 에서 방향을 정해 끝까지 유지 · 본문 스크롤 칸에 `touch-action: pan-y pinch-zoom`(두 손가락 확대는 남김) · 짧아도 빠르게 튕기면 넘김 · 본문이 손가락을 따라 밀리고 넘어갈 만큼 밀면 가장자리 화살표가 채워짐 · 넘기면 빠지고 반대쪽에서 들어옴. 기준값은 `SWIPE_*` 상수, 규칙표는 [docs/IA_5TAB.md](docs/IA_5TAB.md) §5
- **touch-action 은 스크롤 칸에서 새로 시작한다**: 감싼 div 에만 달면 스크롤 칸 안의 터치에는 먹지 않는다(브라우저가 스크롤 칸에서 팬을 다시 허용한다). 그래서 본문 스크롤 칸 두 갈래(병기·단일)에 직접 달았다
- **끄는 동안 다시 그리지 않는다**: 본문은 수백 절이라, 끌 때마다 상태를 바꿔 React 가 다시 그리면 손가락을 못 따라간다. 밀기·화살표는 ref 로 DOM 스타일만 칠한다
- **검증**: 로컬 운영 빌드를 크롬 모바일 흉내(390px)로 띄우고 브라우저 입력 경로(CDP 터치)로 9경우를 확인했다 — 오른쪽 92% 에서 시작한 다음 장 · 끝점 40° 로 휜 엄지 · 세로 스크롤은 장 그대로 · 35px 빠른 튕김은 넘김 / 느리면 안 넘김 · 이전 장 · 가장자리 10px 시작 무시 · 밀었다 되돌리면 취소 · 두 손가락 무시 · 탭은 여전히 절 선택. 가로로 미는 동안 세로 흔들림이 있어도 스크롤 위치가 그대로였다(touch-action 이 먹는다). **실기기(특히 iOS 사파리) 확인은 남았다** — [plan.md](plan.md)
- **함께 바뀐 것**: 두 번 탭 확대가 본문 안에서는 꺼진다(`pan-y pinch-zoom` 에 들어 있지 않다). 본문의 두 번 탭은 원래 절 선택 토글이다. 길게 누르기(전체화면)도 가장자리 20% 에서 되던 것이 24px 제외로 넓어졌다

### 2026-09-14 — 오류로 멈춘 생성 작업이 스스로 일어난다 (그리고 배치 기본값 8)
- **계기**: 지휘부가 엔비디아 드라이버를 올리고 재부팅했다. 그 사이 돌던 작업이 `CUDA error: unknown error` 로 세워졌는데, **다시 켜도 이어지지 않았다** — 432절이 멈춘 채 PC 가 놀았다. 사람이 현황을 조회하고서야 알았다
- **왜 안 이어졌나**: 워커는 `queued`·`running` 만 집는다. `jobs.unfinished()`(다시 켤 때 이어갈 대상)도 `queued`·`running`·`stopped` 만 봤고 **`error` 는 빠져 있었다.** 한 번 error 가 되면 사람이 '이어하기' 를 누를 때까지 영영 멈춰 있었다
- **고침 1 — 다시 켜면 이어간다**: `unfinished()` 에 `error` 를 넣었다. 다시 켜는 것은 프로세스를 새로 띄우는 일이라, 망가진 CUDA 문맥 같은 원인은 그 자체로 사라진다. 그래도 안 되면 같은 자리에서 다시 error 가 되므로 무한히 반복되지 않는다. 멈춘 사유는 `last_error` 로 남긴다
- **고침 2 — 이어가기를 화면에서 떼어냈다**: `jobs.resume_all()` 이 `demo.load`(화면이 열릴 때)에만 걸려 있어, **브라우저가 안 뜨면 이어지지 않았다.** `__main__` 으로 옮겨 창만 켜면 되게 했다
- **고침 3 — 돌고 있는 중에도 스스로 회복**(`fleet._recover_errors`): 사람이 다시 켜 줄 때까지 기다리지 않는다. 두 갈래다 — **CUDA·드라이버 쪽 오류는 프로세스를 다시 켜고**(한 번 망가진 CUDA 문맥은 같은 프로세스에서 되살릴 수 없다), 그 밖은 그 자리에서 다시 큐에 올린다. 한도는 작업당 3번·다시 켜기는 한 시간에 3번이고, 넘으면 손대지 않고 `사람이 봐야 합니다` 를 현황 화면에 띄운다
- **배치 기본값 4 → 8**(지휘부 제안, `jobs.DEFAULT_BATCH`): 모자라면 워커가 그 묶음만 반으로 줄여 다시 만들고 세 번 걸리면 아예 낮추므로 낮게 잡아 손해 볼 이유가 없다. 실측 3060 +8% · 3080 Ti +63%, 그래픽 메모리 최대 9.5GB(12GB 카드), 그리고 **이 저장소에서 메모리 부족이 난 적이 한 번도 없다**(작업 45건에 기록 0). 흩어져 있던 기본값을 상수 한 곳으로 모았다
  - **자동 줄이기가 못 잡는 경우**: 윈도우 드라이버가 예외 대신 시스템 메모리로 흘려보내면 오류 없이 느려지기만 한다. 신호는 현황의 `시간당 절 수` 가 뚝 떨어지는 것 — 그때만 그 PC 를 4 로 내린다
- **코드 지문이 바이트가 아니라 내용을 보게**: 서버가 내려주는 사본은 줄 끝이 LF, git 으로 받는 개발 PC 는 CRLF 인 파일이 섞인다. `prosody.py` 하나 때문에 **같은 코드인데 두 PC 가 다른 것으로 보였다**
- **모든 PC 대상 지시가 한 대에서만 돌던 문제**: 먼저 끝낸 PC 가 `status` 를 done 으로 바꾸면 나머지가 그 명령을 건너뛴다. 두 번 처리를 막는 것은 `takenBy`(PC 마다 한 번)이지 `status` 가 아니다 — `*` 지시는 pending 으로 두고 `doneBy` 를 쌓는다. 대신 `CLAIM_WINDOW_MS`(30분)를 두어 며칠 뒤 켜진 PC 가 묵은 '멈춤' 을 뒤늦게 실행하지 않게 했다


### 2026-09-13 — 사진 올리기가 말없이 끝나던 문제 + 삼켜진 오류의 흔적 남기기
- **계기**: 지휘부가 사진을 찍고 확인을 눌렀는데 **저장 중 표시도 없이 화면만 돌아오고 아무 일도 일어나지 않은** 일이 두 번 있었다고 알렸다
- **원인**(`CardPreview.handleFileSelect`): `try { … } finally { … }` 였고 **`catch` 가 없었다.** 응답 코드가 오는 실패(401·400·500)는 알렸지만, **연결 자체가 끊겨 `fetch` 가 거부되면** 예외가 아무에게도 알려지지 않고 사라졌다. 스피너만 깜빡이고 화면은 그대로다. 사진을 찍는 동안 화면이 꺼졌다 켜지며 통신이 잠깐 끊기는 것은 흔한 일이라 **간헐적으로만** 났다
- **함께 있던 조용한 구멍들**: ① 카메라가 파일 없이 돌아오면 `if (!file) return` 으로 말없이 끝났다 ② 200 인데 본문이 JSON 이 아니거나 `photo` 가 없으면 `photo.id` 에서 터져 역시 사라졌다 ③ `resizeImage` 는 `onload`·`onerror` 가 둘 다 안 오면 **영영 기다렸다**(시간 제한 없음, 객체 URL 도 안 돌려줬다)
- **고침**: 모든 갈래에 말을 붙였다. 바깥 `catch` 추가(연결이 끊긴 경우는 `navigator.onLine` 을 보고 다르게 안내) · 응답 검증 · 파일 없음 안내 · 리사이즈 15초 제한과 URL 반환. 저장소가 막힌 브라우저에서 `sessionStorage` 가 던져 업로드 전체가 죽는 일도 막았다(삼킨다)
- **용량 검사를 리사이즈 뒤로**: 어차피 긴 변 1200px·JPEG 80%로 줄여 보내는데 **원본** 크기로 먼저 쟀다. 요즘 폰 사진은 8~12MB 라 줄이면 넉넉히 통과할 사진을 먼저 거절했다
- **'저장 중' 이라는 말이 없었다**: 상태가 boolean 하나(`cleaningImage`)라 '그냥 사용' 으로 올릴 때도 화면에 `글자 제거 중...` 이 떴다. 단계(`uploadStage`: clean·save)로 바꿔 `사진 저장 중...` 을 보여 준다 — 지휘부가 "저장중 로딩창도 없다" 고 느낀 까닭의 절반은 이것이다
- **앱이 죽었다 되살아난 경우**: 카메라가 떠 있는 동안 안드로이드가 메모리를 회수해 탭을 죽이면 페이지가 새로 뜨고 `onChange` 는 영영 오지 않는다 — 앱이 막을 수 없다. 카메라를 열 때 `sessionStorage` 에 표시를 남기고, **표시가 남은 채로 앱이 새로 뜨면** 알려 준다. 그냥 취소하고 돌아온 경우에는 화면이 다시 보이고 3초 뒤 표시를 치워 헛말을 하지 않는다
- **삼켜진 오류의 흔적**(`components/ErrorTrail.tsx`): 전역 오류 수집이 **하나도 없어서** 이번 일의 흔적이 어디에도 남지 않았다. `unhandledrejection` 과 `error` 를 콘솔에 남기고 최근 스무 건을 `sessionStorage` 에 쌓는다(`yebomErrors()` 로 꺼낸다). **서버로 보내지 않는다** — 오류 메시지에 본문·개인 데이터가 섞일 수 있고, 보내는 순간 그것이 또 하나의 실패 지점이 된다


### 2026-09-13 — 생성 PC 무리: 현황을 한 곳에 모으고, 책을 서버가 나눠 주고, 대화창에서 부린다
- **문제**: 음원 생성은 PC 여러 대가 며칠씩 도는 일인데, 진행 상황이 각 PC 의 `jobs/*.json` 안에만 있었다. 그 PC 앞에 앉지 않으면 어디까지 왔는지·멈췄는지 알 수 없고, 분담은 사람이 `--testament` 로 갈라 줬다 — 한 대를 더 붙이거나 한 대가 꺼질 때마다 다시 나눠야 하고, 범위가 겹치면 **같은 절을 두 대가 만든다**. 보류 절을 서버로 모아 한 화면에서 판단하게 만든 것과 **같은 문제**다
- **현황**(`/api/voice-studio/fleet` · `voice/fleet.py`): 각 PC 가 10초마다 자기 상태를 올린다 — 지금 만드는 절, 남은 절, 시간당 절 수, 책별 진행, 마지막 오류, 맡은 책. 읽기는 관리자 세션 **또는 기기 토큰**(작업 PC 의 명령줄 도구가 쓴다). 쓰기는 자기 자리뿐이라 남의 상태를 망가뜨릴 수 없다 — 토큰 id 는 서버가 정한다
- **지시**(`/api/voice-studio/commands`): 명령을 걸어 두면 PC 가 가져가 실행하고 결과를 적는다. 생성 PC 는 집·사무실 안에 있어 서버가 먼저 연결할 수 없다 — 보류 절 재생성 요청과 같은 방식이다. 집어 간 표시(`takenBy`)를 먼저 남겨 두 번 처리하지 않는다. `stop`·`resume`·`set_batch`·`queue_books`·`queue_replace`·`regen_refs`·`delete_job`·`restart`
- **분담**(`/api/voice-studio/lease`): **책 단위 임대**. PC 가 "이 순서 중 내가 가져가도 되는 책"을 묻고 빌려 가, 끝나면 반납한다. 하트비트가 10분 끊기면 저절로 풀려 다른 PC 가 이어받는다. **진도표 순서는 PC 가 보낸다** — 서버가 따로 가지면 `voice/plan.py` 와 두 벌이 되어 어긋나고, plan.py 는 어차피 서버가 각 PC 로 내려보내는 파일이다. 서버가 하는 일은 겹치지 않게 나누는 것뿐
- **사람이 최종 결정**(지휘부 지시): 자동으로 나누되 관리자 화면에서 **고정**(그 PC 가 계속 맡는다, 꺼져도 안 풀림) · **차단**(아무도 못 가져간다) · **회수**(임대를 풀어 다른 PC 에게)로 조정한다. 조정은 관리자 세션만 — 기기 토큰으로는 안 된다
- **대화창에서 통제**(`voice/fleet_cli.py`): `status`·`books`·`log` 로 보고, `stop`·`resume`·`batch N`·`queue 책,책`·`replace 구약|신약`·`regen "창 1:1,…"` 로 부린다. `--pc` 는 이름 일부만. **대화가 새로 시작돼도 되도록** AGENTS.md 에 "진행 상황을 물으면 추측하지 말고 `fleet_cli.py status` 를 실행한다"고 적었다
- **관리자 화면**(`/admin/voice-fleet`, 설정 → 관리자 → 음원 생성 현황): PC 카드(진행 막대·속도·끝날 때까지)·책 배분·지시 버튼·지시 기록
- **스튜디오 탭 순서 생성 · 검수 · 음원**(지휘부 지시): 열면 생성 화면이 먼저 나온다. 날마다 여는 이유가 생성·확인이고 목소리 등록은 성우마다 한 번뿐이라 맨 뒤로 보냈다. 생성 탭 아래에 **전체 생성**(자동 분담 켜기)과 **모든 PC 현황** 표를 더했다
- **새 성우로 전체를 만드는 길**: 설치(배치 파일 더블클릭 = 설치이자 갱신) → 목소리 등록 한 번 → PC 마다 '전체 생성' 켜기 → 끝. 그다음은 사람이 할 일이 없다. 절차는 [voice/README.md](voice/README.md)
- **찾은 결함 두 개**(만들면서 고침): ① 임대를 물을 때마다 책이 늘었다 — `want` 는 '모두 합쳐 쥐고 싶은 수'인데 순서대로 훑으며 세니 뒤쪽의 내 책이 아직 안 세어졌다 ② 회수된 임대가 '주인 없는 빈 기록'으로 목록에 남았다


### 2026-09-13 — 절 끝이 짧게 끊기는 문제: 끝은 '뽑기'다 — 더 뽑는 것 말고 방법이 없다
- **계기**: 지휘부가 창세기 1장을 듣고 1·2절 끝이 짧게 잘린 것을 지적했다(2026-09-12 항목의 '남은 문제'). 새 방식 8,394절 중 **1,132절(13.5%)이 기준 미달**이었다 — 150ms 미만, 그중 632절은 120ms 미만
- **무엇이 원인이 아닌가 — 실험으로 지웠다**(11.6분, 42회 생성, 결과는 임시 폴더): 끝 공백·끝 줄바꿈·온도 0.55/0.95·강제 조각내기·문장부호 끄기 **어느 것도 효과가 없었다**. 같은 본문을 그대로 두 번 넣어도 230ms 와 40ms 가 나온다. **절 끝 길이는 생성마다 다시 뽑는 제비**이고 조건으로 밀어 올릴 수 없다 — 그러니 답은 '짧으면 다시 뽑기' 하나뿐이다
- **구멍 1 — 통째로 만드는 절은 끝 검사를 아예 건너뛰었다**(`engine.synth_batch`): 쪼개서 만드는 절의 조각에는 끝 검사(`_gen_piece`, 최대 3회)가 붙어 있었는데, 쪼개지 않는 절은 묶음 생성 결과를 그대로 썼다. 대부분의 절이 여기로 간다. 이제 **묶음으로 만든 뒤 끝이 짧은 절만 한 절씩 다시 뽑는다**(같은 규칙·같은 한도). 받아쓰기까지 다시 도는 작업 단계 재시도보다 싸다
- **구멍 2 — 끝 재시도가 받아쓰기 재시도와 예산을 나눠 썼다**(`jobs.py`): 한도가 `retry_max`(3) 하나였다. 한 번에 기준을 넘을 확률이 약 59%이므로 3회면 **6.9%가 짧은 채 남는다**(실측 구약 교체 5,545절 중 6.8% — 계산과 같다). 이제 **끝 전용 한도 `END_RETRY_MAX = 6`** 을 따로 세고(`it["end_tries"]`, `asr_round()`), 계산상 남는 비율이 **0.5%** 로 떨어진다. 전체 생성 시간은 약 5% 는다(기대 시도 1.58회 → 1.69회)
- **둘을 왜 갈랐나**: 성격이 다르다. 받아쓰기 불합격은 회차마다 본문을 더 잘게 쪼개 **조건을 바꾸는** 길이라 몇 번 안에 붙거나 사람이 봐야 한다. 끝 길이는 조건을 바꿔도 나아지지 않는 **제비**라 그냥 더 뽑으면 된다. 섞어 놓으면 끝을 여섯 번 뽑은 절이 받아쓰기 한 번 실패했을 때 곧바로 '마지막 회차' 대접을 받아 지나치게 잘게 쪼개지거나 보류로 떨어진다
- **한도에 닿아도 보류하지 않는다**(기존 규칙 유지): 끝이 가장 긴 시도를 보관해 두었다가 쓴다(`_accept_best`). 내용은 맞고 끝맺음만 아쉬운 것이라 사람이 판단할 일이 아니다. 여섯 번 중 가장 긴 것이므로 세 번 중 가장 긴 것보다 낫다
- **이미 올라간 1,132절**: `lib/voiceStudio/legacy.ts` **세 번째 창**(본문 해시 1,099개 — 같은 본문인 절이 있다)으로 덮어쓰기를 허용하고 다시 만든다. 창세기 258 · 욥기 170 · 출애굽기 149 · 레위기 124 · 민수기 100 · 고린도전서 61 · 요한계시록 45 · 그 밖. **끝이 짧은 순으로** 만들어 중간에 멈춰도 가장 나쁜 것부터 고쳐지게 했다

### 2026-09-12 — 상단 재배치: 조작을 오른쪽으로 모으고 ⋮ 메뉴를 키움 · 하단 탭 찬송가
- **바**: `제목 · 주 역본 · 대역 · 읽기 · ⋮`. 펼쳐보기(전체화면) 아이콘을 바에서 빼 ⋮ 안으로 넣었고, 그 덕에 읽기 버튼이 ⋮ 바로 왼쪽에 붙었다(지휘부: 조작은 엄지 닿는 오른쪽에 모은다)
- **플레이어 줄**: `진행 · 속도 · 성우 · 재생/일시정지 · 접기`. 재생 버튼이 맨 왼쪽에 홀로 있던 것을 성우 칩 오른쪽으로 옮겼다 — 위의 읽기·⋮ 와 같은 쪽이다. 접기는 잘못 누르지 않게 한 칸 띄우고 조금 작게(28px). 떠 있는 미니 플레이어는 그대로(왼쪽 재생 버튼 유지)
- **⋮ 메뉴**: 글자 크기(그대로) · **한 절씩 크게**(옛 '전체화면 1절씩' — 이름을 얻는 것 위주로 바꿨다) · 말씀의삶 · 예배성경 · 앱으로 설치 · 로그인(로그아웃). 설정 시트의 **지름길**이고 설정에서도 그대로 쓸 수 있다 — 본문을 볼 때 하단 탭바가 자동으로 숨기 때문이다. 찬송가는 하단 탭이 되었으니 넣지 않았다. 안드로이드 뒤로가기로 메뉴만 닫는다(`useHardwareBack`)
- **하단 탭**: 네 번째 자리가 말씀의삶 → **찬송가**(지휘부: 예배 중에 더 급히 찾는다). 찬송가는 화면이 아니라 창이라 눌러도 view 가 바뀌지 않는다(설정 탭과 같은 꼴). 말씀의삶은 ⋮ 메뉴로 들어간다 — 진도표를 매일 여는 교인에게는 한 단계 깊어진 셈이다
- **앱 설치 안내**는 설정과 ⋮ 가 같은 코드를 쓴다(`lib/pwaInstall.ts` `promptAppInstall`) — 이미 설치됨·iOS·설치창 거부를 각각 다르게 안내하는 흐름이 두 벌로 갈라지지 않게 했다
- **손잡이를 눌러 펼친 직후의 '유령 클릭' 차단**(2026-09-13 지휘부 보고): 숨은 하단 탭바의 손잡이를 누르면 `pointerdown` 에서 바가 올라오는데, 손을 뗄 때 브라우저가 **그 자리에 지금 있는 요소**로 클릭을 보낸다 — 올라온 탭이 그 클릭을 받는다. 손잡이 알약이 한가운데고 6칸 가운데는 본문·찬송가 경계라 **찬송가가 열려 버렸다**. 한 번 누른 것이 두 곳에 전달되는 것이라 펼친 뒤 500ms 동안 탭을 받지 않게 했다(`BottomTabBar` `REVEAL_TAP_GUARD_MS`). 본문 인라인 플레이어의 접기 손잡이도 구조가 같아(펼치면 진행 막대·속도 칩이 손가락 밑에 온다 → 재생 위치가 튄다) 붙자마자 500ms 는 `pointer-events: none` 으로 둔다
- **빠른 이동 FAB 기본 자리를 아래로**(2026-09-13 지휘부 지시, `QuickNavFab`): 옛 자리(우측 상단 12px)는 3열 헤더의 빈 칸이었는데 한 줄 바로 바뀌며 ⋮ 와 겹쳤다. 기본을 우측 하단(탭바 위)으로 두고, 위로 토글해도 바 아래(64px)에 놓이게 했다. **기본값만 바꾸면 소용이 없었다** — 이 컴포넌트가 마운트할 때마다 지금 값을 저장해 기본값이 곧바로 기기에 박혔기 때문이다(이미 앱을 연 기기는 전부 'tr' 이 저장돼 있었다). 저장은 **사용자가 옮겼을 때만** 하도록 고치고, 저절로 저장된 옛 키는 버리고 새 키(`yebom_quicknav_pos2`)로 갈아탔다
- 전체화면 진입로는 원래 **상단 아이콘과 절 길게 누르기** 둘뿐이었다(설정 시트에는 연결만 남고 버튼이 없다) — 아이콘이 ⋮ 로 들어가도 길게 누르기는 그대로다

### 2026-09-12 — 음원 검수: 일치율만으로는 '통째 누락'을 못 잡는다 (연속 8자 기준 추가)
- **계기**: 지휘부가 창세기 1장을 듣고 1·2절 끝이 짧고 **16절은 '또 별들도 만드셨다'가 통째로 빠진 것**을 찾았다. 앱에서 '음원 다시 만들기'를 걸어 다시 만들었는데 **같은 결함이 또 났다** — 받아쓰기에 그 문장이 없는데 검수는 두 번 모두 90%로 통과시켰다
- **원인**: `qc` 는 받아쓰기와 본문의 **일치율만** 본다(60자 이상 85%, 짧은 절은 더 낮게). 긴 절에서 8~18자가 빠져도 90%가 나와 합격이다. 시스템은 성공으로 알고 지나갔고, 재시도조차 일어나지 않았다
- **고침**(`voice/engine.py` `GAP_MAX`·`text_gap`): 일치율을 통과해도 **한 자리에서 연속 8자 이상 빠졌으면 불합격**. 지휘부가 제안한 '절대 글자 수' 기준을 실측으로 다듬은 것이다 — 새 방식 5,553절에 적용해 보면 **합계로 세면 159절(2.9%)** 이 걸리는데(받아쓰기의 한두 글자 오차가 긴 절에서 쌓인다) **연속으로 세면 55절(1.0%)** 만 걸린다. 문장이 통째로 빠지면 한 자리에서 연달아 빠지므로 '연속' 이라야 실제 누락만 잡힌다. 8자인 이유는 **확인된 누락 중 가장 짧은 것이 8자**('또 별들도 만드셨다')라서다 — 10자로 하면 그 절이 다시 빠져나간다
- **불합격 뒤**: 재시도는 본문을 조각으로 나눠 만든다(`tries ≥ 1` 에서 `force`). 빠진 문장이 자기 조각을 갖게 되어 다시 사라지지 않는다. 세 번까지 안 되면 보류로 올라가 사람이 판단한다
- **이미 올라간 54절**: `lib/voiceStudio/legacy.ts` 에 **두 번째 창**(창 여러 개를 담게 구조를 넓혔다)으로 적어 덮어쓰기를 허용하고 다시 만든다. 창세기 12 · 고린도전서 11 · 요한계시록 9 · 고린도후서 7 · 로마서 3 · 욥기 3 · 그 밖 9권
- **재시도가 조각으로 나누는 기준도 함께 고쳤다**(`engine.comma_split` · `force_split(fine)`): 쉼표 나누기가 '모든 토막이 8자 이상' 일 때만 되던 탓에 고전 1:12(`다름이 아니라,` 7자)은 한 조각으로 남아 세 번 다 같은 자리가 빠졌다. 이제 **짧은 토막은 버리지 않고 옆에 붙이고**, **마지막 시도에서는 문장을 쉼표로 한 번 더** 쪼갠다(문장 안에 열거가 든 고전 6:9 는 3조각 → 7조각). 같은 꼴이 반복되는 열거에서 모델이 한 토막을 통째로 건너뛰는 것이 원인이라, 반복 구절이 서로 다른 조각에 들어가면 건너뛸 것이 없다
- **'이대로 사용' 이 교체를 조용히 건너뛰던 문제도 같이 고쳤다**(`held/action` route): 그 절에 **파일이 없을 때만** 올리게 되어 있어, 교체 대상(구방식 파일이 이미 있는 절)에 관리자가 '이대로 사용' 을 눌러도 아무 일도 일어나지 않았다. 로컬 작업은 '합격·업로드됨' 으로 표시되니 교체가 끝난 것처럼 보였다 — 창세기 16절·욥기 2절이 그렇게 옛 음원으로 남아 있었다(모두 누락 없이 일치율만 낮은 족보 절이라 판단 자체는 옳았다). 이제 구방식이면 덮어쓴다(교체 업로드와 같은 `isLegacyFile` 판정). 남아 있던 18절은 로컬 음원을 교체 경로로 올려 정리했다
- **남은 문제(별건)**: 절 끝이 짧게 끊기는 절 — 2026-09-13 항목에서 다뤘다(조건을 바꿀 방법은 없고, 끝 전용 재시도 한도를 6회로 올리는 것이 답이었다)

### 2026-09-12 — 본문 상단을 한 줄로 · 플레이어를 그 아래 인라인 줄로
- **배경**: 읽기 화면 상단이 두 줄(약 99px)이었다. 위 줄은 브랜드+역본+글자크기(검색·목차와 **공용** 줄), 아래 줄은 목차로+장 이동+읽기. 게다가 재생을 시작하면 떠 있는 미니 플레이어가 우상단을 덮어 역본·글자크기를 `hidden` 처리해야 했다 — 재생 중에는 상단 절반이 빈칸이고 그 위에 알약이 떠 있었다
- **새 한 줄(52px)**: 제목(두 줄로 세움, 탭 → 장 목록) · 주 역본 · 대역 · 읽기(44px) · 전체화면(44px) · ⋮(32px). 검색·목차 화면의 공용 줄은 그대로 둔다(`!isReadingView` 조건) — 그 화면에는 책 제목도 읽기도 없다
- **책 이름 규칙**(`lib/books.ts` `bookBarName`): 390px 한 줄에서 제목에 남는 폭이 약 90px(세 글자 47px + 장 번호 24px)이라, **세 글자까지는 이름 그대로**(창세기·신명기·시편), **네 글자 이상은 약어**(데살로니가전서 → 살전, 마태복음 → 마). 66권 중 33권이 약어가 된다. 교회가 성경 인용에 쓰는 그 표기이고, 전체 이름은 목차·책갈피에 그대로 남는다
- **장 이동 화살표 제거**(지휘부 결정): 화살표 + 역본 칩 2개 + 아이콘 3개는 고정 폭만 320px이라 제목에 38px밖에 남지 않았다(한 글자도 못 들어간다). 이동은 본문 스와이프·제목 탭·하단 목차 탭·PC 좌우 여백 클릭으로 한다
- **⋮ 메뉴**: 지금은 글자 크기만. 앞으로 상단에서 내려올 기능이 모이는 자리(지휘부 지시)
- **플레이어 인라인화**(`TTSMiniPlayer` `inline`): 상단 바 아래 한 줄 — 재생/일시정지 · 진행 · 속도 · 성우 · 접기(▾) + 진행 막대. **접기는 줄만 접고 소리는 계속 난다**. 접으면 하단 탭바와 같은 손잡이(36×4)만 남는다 — 교인이 이미 아는 모양이라 따로 배울 것이 없다. **읽기를 끝내는 길은 상단 정지 아이콘 하나뿐**(옛 ✕는 종료였으므로 뜻이 달라진 만큼 모양을 ▾로 바꿨다). 인라인이 떠 있으면 떠다니는 미니 플레이어는 스스로 물러난다(`inlinePlayerActive`), 전체화면에서는 그쪽 도킹 플레이어가 맡는다
- **진행도를 컨텍스트로 흘리지 않는다**: `timeupdate` 는 초당 4회 넘게 온다. 컨텍스트 값에 담으면 이 컨텍스트를 쓰는 본문 화면(SearchPanel, 수백 절)까지 그때마다 다시 그려진다. `subscribeAudio` 로 **플레이어만** 오디오 엘리먼트를 구독해 다시 그리는 범위를 한 줄로 묶었다. 탐색도 구독으로 받은 엘리먼트에 직접 한다
- **진행 막대의 단위는 두 가지**: 장 통째 녹음(개역·통독)은 시간(`0:42 / 3:18`, 막대 탭으로 탐색), 절 단위 낭독(새번역 등)은 절(`3절`, 막대 탭으로 절 이동). 절 모드에 시간을 보이면 '한 절 안의 시간'이라 엉뚱하다
- **본문 높이 상수 갱신**(`browseMaxHClass`): 대기 160/104 · 재생·펼침 226/170 · 재생·접힘 188/132(탭바 보임/숨김). **이 상수를 함께 고치지 않으면 줄을 줄여도 본문은 넓어지지 않는다.** 옛 값에서 계산한 추정치라 기기에서 보고 다듬을 수 있다

### 2026-09-12 — SSO 로그인: 세션을 한 곳으로 · 체크인으로 연장 · 로그아웃 정리 · 공용기기 혼입 차단
- **세션 단일화**(`contexts/SessionContext`): `useSession` 이 컴포넌트마다 자기 상태를 들고 있어 로그아웃이 다른 화면에 전달되지 않았다 — 특히 `LoginGateProvider` 의 사본이 남아 **로그아웃 뒤에도 `ensureLogin()` 이 통과**했다. provider 하나로 모으고 `hooks/useSession` 은 얇은 재수출로 남겼다(호출부 7곳 그대로). 반환값에 `deviceReady` 추가
- **체크인이 곧 연장**: 교적부 `GET /api/auth/session` 은 '로그인 유지' 세션을 하루 한 번 다시 봉인해(400일) 내려보낸다 — **세션을 늘리는 수단은 이것뿐**이다. 앱을 열 때·포커스/가시성 복귀·bfcache 복귀에 부르고 45초 최소 간격을 둔다(교적부에 호출 제한이 없고 매 호출이 DB 왕복). 판정은 **HTTP 상태로만** — 교적부는 500 에도 본문에 `{isLoggedIn:false}` 를 담아서, 본문을 믿으면 교적부가 아플 때 교인이 통째로 로그아웃된다. 401 만 익명 전환, 500·네트워크는 그대로 둔다
- **콜드 스타트**: "오류면 기존 상태 유지" 는 앱을 처음 열 때 지킬 상태가 없다. 같은 오리진 `/api/auth/session`(쿠키 복호화)으로 잠정 상태를 먼저 잡고 체크인으로 확정한다 — 교적부 장애에도 유효한 쿠키가 익명으로 보이지 않고 첫 화면 깜빡임도 없다. 잠정 상태로는 기기 정리 판정(`deviceReady`)을 켜지 않는다. `localhost`·프리뷰는 쿠키가 실리지 않아(다른 사이트) 체크인을 아예 부르지 않는다
- **로그아웃**(5개 앱 공통 순서): 확인창 → 저장소 정리 → 교적부 통합 로그아웃. 정리는 **개인 데이터만**(`lib/auth/clear-device`) — 글꼴·테마·역본·성우 11종 같은 기기 취향은 떠 두었다 되돌리고, 공개 성경 음원 캐시(IndexedDB `yebom_tts_cache`)는 지우지 않는다(개인정보 아님, 지우면 데이터 요금만). 통합 로그아웃의 실제 삭제는 same-site 이동에서만 듣는다 → 검증은 프로덕션에서만 유효
- **공용기기 혼입 차단**(`lib/auth/device-owner`): 로그인 동기화는 양방향이라 **판정이 동기화보다 먼저** 끝나야 한다. 같은 사용자면 그대로, 처음 보는 기기에 로컬 책갈피가 있으면 합칠지 한 번 묻고, 다른 사용자면 지운다. 그룹 초대코드는 누가 넣었는지가 없어 남의 그룹 자동참여로 이어지므로 **30분 유효** + 로그아웃·401 때 삭제. 동기화·스크랩 이관·그룹 자동참여는 `deviceReady` 를 기다린다
- **관리자 서버 재검증**(`lib/auth/verifySession`): 이 앱 API 는 쿠키를 스스로 복호화해서만 신뢰하는데, 교적부가 봉인한 '로그인 유지' 쿠키는 **400일**이고 이 저장소는 음원 PC 등록 때 쿠키 복사를 안내한다 → 밖으로 나간 사본이 오래 통한다. 되돌리기 어려운 관리자 기능(본문 전역 수정·보류 음원 영구 삭제·**180일 기기 토큰 발급**)에만 공유 DB 재검증을 붙였다 — `session_version` 불일치 거부(강제 로그아웃 즉시 반영), 승인 취소 거부, 등급은 DB 최신값. 조회 실패는 통과가 아니라 503, 같은 사용자 60초 캐시. 읽기·쓰기 핫패스는 세션 판독이 라우트마다 복사돼 있어 헬퍼 통합 뒤로 미뤘다(plan.md)
- **관리자 이메일 화이트리스트 제거**(`lib/admin.ts`): 소스에 박힌 기본 이메일은 교적부에 **없는 계정**이라 아무 권한도 주지 않으면서, 그 이메일이 나중에 등록되면 교적부가 회수할 수 없는 관리자가 되는 잠복 경로였다(게다가 `NEXT_PUBLIC_` 이라 클라이언트 번들 노출). 관리자 권한은 이제 교적부 `permission_level` 에서만 나온다(실측 admin 10 · super_admin 1). 비상 진입로는 서버 전용 `ADMIN_EMAILS`(기본값 없음)
- **무인증 유료 엔드포인트 호출 제한**(`lib/rateLimit`): 카드 만들기의 AI 배경·이미지 정리·주제 추천·사진 검색은 비로그인 교인도 써야 해 인증을 걸지 않는 대신, 주소별 10분 창으로 상한을 뒀다(10·20·30·30·90). 인스턴스 메모리라 실제 한도는 그 배수 — '무제한'을 '느슨한 상한'으로 바꾸는 것이 목적

### 2026-09-12 — 구방식 교체 사고: 로컬 옛 음원을 '교체본'으로 올림 → 고침·복구
- **무슨 일**: 이 PC 가 구약 교체로 넘어간 직후(00:36 KST) 욥기·창세기 교체 작업이 만들 절 없이 '합격'으로 떴다. `jobs.new_job` 은 로컬에 파일이 있으면 '기존 파일'로 합격 처리하는데(재개용) 교체 작업도 그랬다 — 구방식 욥기·창세기는 이 PC 가 만들어 로컬 파일이 곧 옛 음원이다. 워커가 그것을 교체 업로드로 올려 서버가 새 방식으로 여기게 됐다: 욥 1:1~26:1 149절(본문 해시 139개, 서버 구방식 목록 9,816 → 9,677). 약 1분 만에 워커를 멈췄고 창세기는 올라가기 전이었다
- **고침(생성 PC)**: 교체 작업은 로컬 파일을 건너뛰지 않는다(`new_job`). 이미 걸린 교체 작업의 '기존 파일' 절은 작업을 시작할 때 만들 절로 되돌린다(`_skip_already_replaced` — 서버 목록을 못 받아도 먼저). 새 PC 에 걸어 둔 신약 교체도 스튜디오를 다시 켜 고친 코드로 돌면 시작할 때 바로잡힌다
- **복구(서버)**: 139개 키를 '구방식이 그대로 다시 올라간 파일'로 기록했다(`lib/voiceStudio/legacy.ts` `LEGACY_REUPLOADED` — 기록 시각 00:40 KST 이전에 올라온 것이면 구방식). cache-index `?legacy=1` 과 교체 업로드가 같은 `isLegacyFile` 로 판정해, 교체 작업이 이 절들을 다시 만들고 덮어쓸 수 있다. 새 방식으로 올라가면 시각이 새로워져 목록에서 저절로 빠진다

### 2026-09-11 — 구방식 음원 교체 순서 변경: 이 PC 구약 먼저 · 스튜디오 '교체 작업 걷기'
- **배경**: 말씀의삶 1기가 구약(욥기·창세기…)부터 읽는다. 처음 분담은 각자 맡은 쪽 교체(이 PC 신약 / 새 PC 구약)였는데, 빠른 이 PC 가 신약 남은 절(요한계시록)을 마치면 **구약** 구방식 교체를 진도표 순서로 하고, 새 PC 는 구약 남은 절을 마친 뒤 **신약** 교체를 하도록 바꿨다
- **이 PC**: 바탕화면 '성경 음원 이어하기' — `run_plan.py --testament new` 가 정상으로 끝나면(종료 코드 0) `--testament old --replace-legacy`. 중지 배치는 워커를 강제 종료(코드 1)하므로 멈추면 다음 단계로 넘어가지 않는다. 교체 단계는 창 제목이 '구약 구방식 음원 교체'. 옛 '신약 음원 이어하기/중지' 는 지웠다(끝나면 신약 교체로 넘어가 새 분담과 어긋남)
- **스튜디오 '교체 작업 걷기'**(생성 탭, '구방식 교체 작업 걸기' 옆): 교체할 쪽(구약/신약)의 교체 작업 중 아직 만들 절이 남은 것만 지운다(`jobs.delete_job`, `app.ui_withdraw_replacement` — 제목의 책 이름을 진도표로 구약/신약 판정). 지금 도는 작업은 지우지 않고 알린다(워커가 다음 저장 때 되살림). 끝난 교체 작업은 기록으로 남긴다. 새 PC 에 예전 지시대로 걸어 둔 구약 교체를 거두는 데 쓴다 — 겹쳐도 서버는 구방식 파일만 덮어써 사고는 아니지만 두 PC 가 같은 책을 만드는 헛수고다
- 새 PC 의 구약 남은 절과 이 PC 의 구약 교체는 겹치지 않는다(남은 절 = 서버에 파일 없는 절, 교체 = 구방식 파일이 있는 절)

### 2026-09-11 — 음원 생성: 스튜디오 화면 가볍게 · 도는 작업의 배치 바꾸기 · 속도 측정 도구
- **배경**: 새 PC(Dell Precision 7910, Xeon E5-2697 v4 ×2, RTX 3080 Ti 12GB)가 이 PC(i9-10900F, RTX 3060 12GB)보다 약 3배 느리다. 우리 엔진은 GPU 가 아니라 **CPU 코어 하나**가 속도를 정한다 — 이 PC 측정: 워커 CPU 정확히 1.00코어, GPU 35~40%·40W. 소리 1초에 약 200번(12.5프레임 × 본 모델 28층 1번 + 보조 모델 5층 15번) 순차 계산하고, 걸음마다 파이썬이 GPU 에 작은 일을 넘긴다(qwen_tts 가 프레임마다 `code_predictor.generate` 를 새로 부르고 CUDA Graph·compile 없음). 그래서 코어 수가 아니라 코어 하나의 속도(부스트 클럭 × 세대)가 좌우한다. 새 PC 는 터보 정상(생성 코어 3.43GHz, 이 PC 4.7GHz)이고 GPU 는 생성 코어와 같은 CPU(NUMA 0) — 하드웨어로 설명되는 차이는 약 1.5배
- **스튜디오 화면**: 3초마다 작업 목록·책별 진행을 다시 그리며 모든 작업 파일을 두 번, 지금 작업 파일을 두 번 새로 읽었다. 같은 프로세스라 읽는 동안 생성이 멈춘다(구약을 한 작업으로 걸면 ≈ 9MB, 한 번 약 0.09초). 화면용 읽기는 바뀐 파일만 다시 읽는다(`jobs.list_jobs_view`/`load_view` — 파일 시각·크기로 판단, 돌려준 dict 는 공유라 고치지 않음). 워커·저장 쪽은 그대로(`list_jobs`/`load`)
- **도는 작업의 배치 바꾸기**: 배치 슬라이더는 새로 거는 작업에만 쓰였다. 생성 탭 **'배치를 위 값으로'** — 고른 작업(비우면 지금 도는 작업)에 요청을 남기면(`batch_requests.json`, jobs/ 한 칸 위) 워커가 다음 묶음 전에 반영한다. 작업 파일을 직접 고치지 않는 건 도는 워커가 다음 저장 때 덮어쓰기 때문. 워커 줄에 지금 배치가 보인다
- **그래픽 메모리 부족**: 묶음 생성이 메모리 부족으로 실패해도 작업이 멈추지 않고 배치를 줄여 같은 절을 다시 만든다(아래 '메모리 부족은 그 묶음만'). 전엔 작업이 '오류'로 멈춰 누가 이어하기를 누를 때까지 서 있었다. 사유는 워커 줄에 보인다
- **속도 측정 `bench.py`**: 고정 새번역 8절로 배치 1·4·8 의 시간당 절 수·그래픽 메모리, `--pair` 는 같은 GPU 에 생성 줄기 2개를 동시에(각 배치 4). 결과는 화면과 `bench/`(JSON). 생성(스튜디오 창·run_plan)이 돌면 멈추고 잰다. 설치 PC 도 켤 때 받는다(`/api/voice-studio/code` 목록)
- 참고 — 긴 절(120자 초과)과 재시도는 조각마다 배치 1로 만든다(`synth_batch` 의 나눠 만들기 경로, 조각 끝이 짧으면 최대 3번). 배치를 키워도 이 경로는 빨라지지 않는다 — 이름·족보가 많은 구약이 느린 한 이유
- **측정 결과(이 PC, 생성만)**: 배치 1·4·8 = 시간당 139·442·476절, 모델 쪽 메모리 4.6·6.7·9.5GB, 받아쓰기 검수 한 절 0.6초. **생성 줄기 2개 동시(각 배치 4)는 합계 171절** — 12GB 를 꽉 채워(11.7GB) 서로 밀어내며 한 줄기보다 느려진다(받아쓰기 한 절 14.8초) → 한 PC 한 줄기 유지('워커는 한 번에 하나만' 을 실측으로 확인). 실제 작업(시간당 150~180절)을 깎는 것은 재시도·긴 절의 배치 1 경로(`voice/README.md` 새 방식 속도)
- **새 PC 측정(같은 8절)**: 배치 1·4·8 = 시간당 106·305·**498**절, 받아쓰기 한 절 0.9초. 배치 1·4 는 CPU 코어 하나가 정해 이 PC 가 1.3~1.45배 빠르지만, 배치 8 은 한 걸음의 GPU 일이 커져 3080 Ti 가 이 PC(476)를 앞선다(3060 은 이미 한계라 +8%). 하드웨어 차이는 1.3~1.45배뿐 — 실제 작업의 약 3배 차이는 스튜디오 화면 부담(이번에 고침)과 구약의 재시도·긴 절. **CPU 업그레이드는 불필요 → 새 PC 배치 8, 이 PC 배치 4**
- **메모리 부족은 그 묶음만**: 배치 8 은 12GB 에 여유가 적어(모델 쪽 9.5GB + 받아쓰기·CUDA 약 1.2GB) 긴 절이 몰린 묶음에서 모자랄 수 있다. 처음 만든 '배치를 반으로 영구히 줄임'은 한 번만 모자라도 배치 8 의 이득을 잃어, **그 묶음만** 반으로 만들고 다음 묶음은 원래대로, 세 번째부터 아예 줄이게 바꿨다. 배치 바꾸기 요청이 오면 제한도 푼다(`jobs._process` `oom_cap`)

### 2026-09-11 — 보류 절 검수 화면 정리
- '음원 다시 만들기 요청' 목록을 **'음원 다시 만들기 기록'** 으로 — 할 일이 아니라 요청 기록(대기·만드는 중·완료)이다. 완료는 흐리게, "‘완료’는 이미 새 음원으로 바뀐 기록" 안내. 롬 3:10 '시험' 줄이 할 일처럼 보여 혼동됐다(그 시험 기록은 지움)
- 판단 대기 │ 처리됨 사이에 끼어 있던 중복 '새로고침' 버튼을 뺐다(오른쪽 것과 같은 동작)

### 2026-09-11 — 말씀의삶: 진입 때 그룹 초대코드 창
- **무엇**: 말씀의삶에 들어오면 한 번 "함께 읽는 그룹이 있나요?" 창(`GroupCodePrompt`). 코드를 넣으면 참여, 비운 채 확인·창 바깥·뒤로가기·'그룹 없이 들어가기'면 그냥 진도표(로그인했으면 개인 진도, 아니면 보기만). 전엔 그룹 탭 안 '초대코드로 참여'로만 참여할 수 있어 찾기 어려웠다
- **한 번만**: 참여했거나 한 번 건너뛰면 그 기기에서 다시 묻지 않는다(`yebom_plan_group_prompt`). 로그인했고 이미 그룹이 있으면(다른 기기에서 참여) 묻지 않는다
- **비로그인**: 코드를 넣으면 기기에 적어 두고(`yebom_plan_pending_group_code`) 로그인으로 — 돌아와 말씀의삶에 들어오면 자동 참여(실패하면 이유와 함께 창을 다시 연다)
- 로그인·그룹의 관계(비로그인 = 보기만 / 로그인 = 진도 기록 / 그룹 = 서로의 진도)는 그대로. 로그인 유지 기간(지금 7일, 교적부가 발급하는 공용 쿠키)은 교적부 쪽에서 다룬다
- 상세: [docs/READING_PLAN.md](docs/READING_PLAN.md) §9

### 2026-09-11 — 성우 목록 개편: 새번역 5종, 개역·통독에도 성우 선택
- **새번역**: 영희·생생·김단아 │ 쾌활·천사장(`KOREAN_VOICE_ORDER`). 생생(f2)·쾌활(m2, 예전 '활력')은 9/10 에 띄어쓰기가 어색해 뺐다가 쉼표 쉼(아래 항목) 뒤로 되살렸다. 지성·감미·품격·할부지(Supertone)는 목록에서 뺐다(`RETIRED_KOREAN_VOICES`, 서버 설정은 남김 — 이날 Supertone 은 오류 상태)
- **개역·통독**: 읽기를 누르면 성우를 고를 수 있다 — 생생 · 쾌활 · 성우(사람 녹음, 장 통째). 새번역 선택과 따로 기억하고(`recordedVoice`, `yebom_tts_recorded_voice`) 기본은 지금까지처럼 성우. 생생·쾌활이면 녹음을 찾지 않고 절 단위로 읽는다(`wantsRecording` — 시작·자동 다음 장 모두). 성우를 골라도 녹음이 없는 장과 전체화면은 생생. 재생 중 녹음 ↔ AI 를 바꾸면 지금 장을 새 방식으로 처음부터 다시(장 통째 녹음은 절 위치로 갈 수 없음), 생생 ↔ 쾌활은 다음 절부터
- **옛 저장값**: 목록 개편 때 한 번(`VOICE_LINEUP`) 생생·활력·Supertone 을 골라 둔 옛 저장값을 지운다 — 9/10 부터 영희로 듣던 분이 갑자기 옛 선택으로 돌아가지 않게
- **캐시 칸**: 생생·쾌활은 쉼표 쉼 전 음원이 서버(R2 `f2`/`m2`)와 기기에 남아 있어 새 칸 `f2-p`·`m2-p` 를 쓴다(`route.ts` `koVoiceCacheSlot`, `TtsContext` `cacheVoiceSlot`). 생생·쾌활의 Neural2·WaveNet 폴백 음원은 기기에 저장하지 않는다
- 화면: 미니 플레이어 성우 팝오버(개역·통독은 녹음 재생 중에도 표시), 설정 → 음성 '새번역 성우' / '개역·통독 성우'

### 2026-09-11 — 한국어 Chirp 가 쉼표에서 쉬게
- **증상**: Chirp(대신 읽기의 마지막 단계)가 쉼표에서 쉬지 않고 급히 지나가 띄어쓰기가 어색하다
- **원인**: Google 한국어 Chirp3-HD 음성은 쉼표를 무시한다(2025-08 Google 개발자 포럼 보고 — 우회만 권하고 고친다는 약속 없음). 새번역 7절 시험에서 쉼표에서 쉬기도 하고 안 쉬기도 했고(시 23:1 쉼 없음), 대신 엉뚱한 곳에서 0.25~0.35초 쉬었다(요 3:16·눅 2:14)
- **해결**: 한국어 Chirp 요청을 `markup` 입력으로 보내고 쉼표마다 `[pause short]` — 쉼표마다 0.4~0.6초, 엉뚱한 쉼이 사라짐(`route.ts` `koChirpMarkup`, 사용자가 샘플 비교 후 선택). 마침표는 원래 쉬어서 그대로(태그를 더하면 0.85초로 과함). SSML `<break>` 는 0.7~1초로 과하고 미리보기 기능이라 안 씀. 본문의 `[ ]`(새번역 88절)는 괄호째 보내면 이상하게 읽혀(마 18:15 1.5초 길어짐) 괄호 글자만 뺀다. 쉼 표시가 거절되면 글자로 다시 요청
- 대신 읽은 음원은 기기에 저장되지 않으므로(아래 항목) 배포 즉시 적용

### 2026-09-11 — 성우 대신 읽기 순서 통일 + 대신 읽은 음원은 기기에 저장 안 함
- **순서(모든 성우 같은 규칙)**: 고른 성우(음원 → 생성) → 영희 음원 → 김단아(음원 → 11labs 생성) → GCP Chirp3-HD(고른 성우의 성별) → Neural2 → WaveNet. 전엔 영희만 김단아로 대신 읽고 나머지는 곧장 Chirp 라, Supertone(지성·감미·품격·할부지)이 오류일 때 새번역도 Chirp 로 읽혔다. `route.ts` `KOREAN_STAND_INS`·`synthLive`
- **돌고 돌지 않음**: 영희↔김단아처럼 서로를 대신해도, 한 요청에서 목록을 한 번 훑을 뿐(각 단계 한 번씩) 성우끼리 서로를 통째로 부르지 않는다. 천사장이 토큰·접속 문제로 실패하면 같은 11labs 계정이라 브레이커(10분)가 걸려 김단아 생성은 건너뛴다(김단아 음원만 쓰임)
- **서버 저장**: 생성 성공은 그 성우의 키로(그 성우의 진짜 음원이라 공유), 대신 읽은 음원을 다른 성우 키로 저장하지 않는다. 영희 키엔 라이브 산출물을 절대 저장하지 않는다
- **기기 저장**: 대신 읽은 음원(고른 성우의 표지가 아닌 것 — 영희 `voice:f4`, 김단아 `el:…`, GCP `ko-KR-…`)은 기기에 저장하지 않고 이미 저장된 것도 무시한다(`TtsContext` `isStandInAudio`). 전엔 영희만 막아서, 김단아·천사장 등을 고른 기기는 엔진 장애 때 받은 Chirp 음원을 엔진이 돌아온 뒤에도 계속 틀었다

### 2026-09-11 — 고른 절부터 읽기
- **읽기 버튼**: 본문에서 절을 고른 채 읽기(TTS)를 누르면 1절이 아니라 **고른 절부터** 읽는다. 여럿이면 읽는 순서로 맨 앞 절(진도표가 장을 나눠 읽는 회차는 그 순서). 역본은 보지 않는다 — 병기 화면에서 아래 줄을 골라도 같은 절. `SearchPanel.handleTtsToggle`
- **절 단위 음원일 때만**: 장 통째 녹음 음원(개역개정·쉬운성경·WEB)은 mp3 안에서 절로 갈 수 없어 장 처음부터 — "녹음 음원은 장 처음부터 재생됩니다" 알림, 선택은 그대로. 이를 가리려고 `tts.start()` 가 절 단위 재생 여부를 돌려준다(`Promise<boolean>`)
- **선택 풀기**: 절 단위로 시작하면 선택을 푼다(복사와 같은 규칙 — 읽는 절 표시가 위치를 대신한다). 다른 장의 절까지 모아 두는 중이면 선택을 그대로 둔다
- **처음부터 │ 확인**: 시작하면 `N절부터 읽습니다` 알림 아래 두 버튼이 2초 동안 뜬다. 그냥 두거나 '확인'이면 고른 절부터 그대로, '처음부터'면 장 처음(진도표 순서의 첫 절)부터 다시 읽는다. 읽기 버튼으로 멈추면 알림도 닫는다

### 2026-09-11 — 음원 생성 PC 실행 파일의 오류 메시지
- **증상**: 새 PC 바탕화면 `예봄성경 음원생성.bat` 을 켜면 `액세스가 거부되었습니다.`, `'?깃꼍'은(는) 내부 또는 외부 명령…`, 'Windows 보안 — 파일을 복사할 수 없음(인터넷 보안 설정)' 창이 뜬다. 스튜디오 자체는 정상 동작
- **원인**: 설치 때 실행 파일을 바탕화면에 **글자로** 옮겨 적으면서 줄 끝이 LF 가 됐다(`read_text` 가 CRLF→LF). LF + `chcp 65001` + 한글 창 제목 줄에서 cmd 가 줄을 잘못 끊어 조각을 명령으로 실행한다 — '?깃꼍' 은 '예봄성경' 바이트의 중간, 접근 거부·Windows 보안 창은 주소 조각(`//bible.yebom.org`)을 네트워크 경로로 열려던 것으로 본다. 이 PC 시험에서 같은 모양의 LF 파일은 조각 실행·`pause`·무한 대기까지 보였고 CRLF 판은 정상. 설치 배치(CRLF)는 시험에서 정상이라 그대로 둠
- **해결**: 실행 파일을 CRLF·영문만으로(`bootstrap.launcher_bytes` — 경로는 `%LOCALAPPDATA%` 로, 한글 창 제목은 파이썬이), 바탕화면에는 바이트 그대로 복사. 이미 설치된 PC 는 켤 때 고친다(`repair_launchers` — `--run` 과 `app.py` 시작 시. 도는 중인 옛 파일은 새 내용이 cmd 가 이어 읽을 자리보다 짧을 때만 바꿔 써서 파이썬이 끝나면 그냥 끝나게). `bootstrap.py` 를 `/api/voice-studio/code` 목록에 넣어 켤 때마다 새로 받게 했다(전엔 설치 때 한 번뿐이라 여기를 고쳐도 설치된 PC 에 닿지 않았다) — 옛 설치도 한 번 켜면 고쳐진다
- **곁가지**: 스튜디오를 켤 때 브라우저를 두 번 열던 것(bootstrap 이 서버가 뜨기 전에 한 번 + gradio `inbrowser`)을 gradio 한 번으로. gradio 가 새 starlette 에서 찍는 `HTTP_422_UNPROCESSABLE_ENTITY` 사용 중단 경고는 끔

### 2026-09-11 — 절 음원 다시 만들기 요청 + 절 선택 팝업 개선
- **음원 다시 만들기(관리자)**: 절 선택 팝업에 '음원 다시 만들기'(새번역 절). 서버에 요청을 남기면(`voice-studio/verse-regen/{voiceKey}/{책}.{장}.{절}.json`) 생성 PC 가 10분마다 가져가(`verse-regen/claim`, 먼저 가져가는 PC 가 맡음) 지금 작업을 한 묶음 뒤 잠시 비켜 두고 요청 절부터 새 방식으로 만든다. 올릴 때 요청 id 를 실어 **새 방식 파일이라도 덮어쓴다**(업로드 라우트가 그 기기가 맡은 요청인지 확인). 결과는 `verse-regen/done`(완료·보류) — 받아쓰기에 떨어지면 보류 절 검수로. 관리자 화면 '보류 절 검수' 아래에 요청 목록
- **본문 수정 → 자동 요청**: 관리자가 새번역 본문을 고쳐 저장하면(`/api/admin/verse`) 음원 다시 만들기가 자동으로 요청된다 — 본문이 바뀌면 음원 열쇠(본문 해시)가 바뀌어 대신 읽는 목소리로 넘어가기 때문(롬 3:10·3:13)
- **절 선택 팝업**: 아래 칸을 `선택 N │ 해제` 로 나눴다 — 해제는 한 번에 모두, 5초 동안 '되돌리기'. 복사·메모 저장·수정 저장·음원 다시 만들기 요청 뒤에는 선택이 자동으로 풀린다(색칠하기처럼). '본문으로'는 선택 유지(본문에서 위치 표시)
- **생성 PC**: 요청 절은 서버에 음원이 있어도 건너뛰지 않는다. 할 작업이 없을 때도 요청은 받는다

### 2026-09-11 — 영희의 대신 읽기 음원을 기기에 저장하지 않음
- **증상**: 롬 3:10·3:13 이 서버엔 영희 음원이 있는데(09:49 업로드) 휴대폰에서 김단아로 나왔다
- **원인**: 기기 캐시(IndexedDB) 키는 `version-book-ch-vs-voice` 라 **본문이 없다**. 영희 음원이 아직 없을 때 들은 절은 서버가 김단아로 대신 읽어 주는데, 그 음원이 기기에 저장되면 나중에 영희 음원이 생겨도 그 기기는 계속 김단아를 튼다. 기본 성우가 영희가 된 뒤로는 교인 누구에게나 생긴다(구약 대부분이 아직 생성 중)
- **해결**: 사전 생성 성우(f4)를 골랐는데 서버가 준 음원의 `X-TTS-Voice` 가 `voice:f4` 가 아니면 대신 읽기 음원으로 보고 기기에 저장하지 않는다(이 세션 메모리에만 — 절 사이 끊김 방지). 이미 저장된 대신 읽기 음원도 무시하고 서버에 다시 묻는다 — `DB_VERSION` 을 올리지 않아도 영희 음원이 생기는 즉시 모든 기기에서 영희로 나온다. `TtsContext` `isPregeneratedFallback` / `rememberAudio`

### 2026-09-11 — 음원 보류 검수: 관리자 화면에 보류가 안 보이던 문제
- **증상**: 새 PC 생성 탭엔 보류가 있는데 설정 → '음원보류절 검수'는 "전부 정상". 이 PC 의 갈 3:7 보류도 사라짐
- **원인 1 — 보고 크기**: 보류 목록에 음원을 전부 실어 한 요청으로 보냈다. 새 PC 보류 22개 음원만 base64 4.08MB — 서버 요청 한도(4.5MB)에 걸려 2026-09-10 오후부터 보고가 통째로 거절됐다
- **원인 2 — 덮어쓰기**: 서버는 PC 마다 목록을 통째로 갈아끼우는데, PC 는 **지금 작업(책)의 보류만** 보냈다. 보류 0 인 책이 끝나면 앞 책의 보류가 지워졌다
- **원인 3 — 판단 반영**: 재생성 요청은 그 절이 든 작업이 **끝날 때**만 확인했다(끝난 책은 영영, 구약을 한 작업으로 도는 PC 는 며칠 뒤). '이대로 사용'은 PC 에 알려지지 않아 생성 탭 보류 숫자가 줄지 않았다
- **해결**: 목록은 음원 없이 보내고 서버에 없는 음원만 한 건씩(`POST held/audio`, 응답 `missingAudio`). 들을 수 있는지는 관리자 화면이 실제 파일로 판단. PC 는 보류를 작업 전부에서 모아 보고. 10분마다 관리자 판단을 모든 작업에 반영 — 재생성 요청은 다시 만들고(끝난 작업은 큐에 다시), 이대로 사용은 합격·업로드됨으로 표시(`held/tasks` 가 `decided` 도 준다)
- **로컬 스튜디오 '3. 검수' 탭**: 이 PC 작업 파일의 절별 결과 — 행을 눌러 들을 수 있다. 판단은 관리자 화면에서 한다(탭에 안내 문구)
- **후속 6 — 옛 판단이 새 음원에 적용**: 마 1:5·눅 3:27·10:27 은 아침에 옛 음원으로 '이대로 사용'을 눌렀는데, 새 방식으로 다시 만들어 또 보류되자 PC 가 그 판단을 새 음원에 적용해 '완료'로 만들었다(서버엔 음원 없음, 관리자 화면에선 '처리됨'으로 빠짐). 판단 기록에 음원 지문(`audioSec|asr`)을 남기고, 지금 보류 항목과 다르면 관리자 화면·PC 모두 그 판단을 버린다(`actionIsCurrent`). 음원 파일 시각으로 가르지 않는 건 음원 표지 도입 때 모든 보류 음원이 한 번 다시 올라가 기존 판단이 전부 무효가 되기 때문
- **후속 5 — 나눠 만든 절의 조각 끝**: 새 방식도 나눠 만들면 조각 끝(= 절 중간 문장끝)이 짧게 나오는 일이 있다. 절 끝 검사는 음원 전체의 마지막만, 받아쓰기 합격 때만 봐서 중간 조각 끝은 검사된 적이 없었다 — 이 PC 새 방식 합격 1,466절 중 나눠 만든 60절, 그중 23절이 중간 조각 끝 150ms 미만. 조각마다 끝을 재서 짧으면 그 조각만 다시 뽑는다(`engine._gen_piece`, 최대 3회·가장 긴 것)
- **후속 4 — 다시 보류된 절의 옛 음원**: 욥 39:8 을 새 방식으로 다시 만들었는데 다시 보류되자, 서버에는 9/9 구방식 보류 음원이 그대로 남았다(같은 본문 = 같은 id 라 '이미 있음'). PC 가 보류 음원의 표지(크기-수정시각)를 보내고, 서버는 음원을 받을 때 표지를 적어 두었다가(`held/{기기}/stamps.json`) 다르면 다시 받는다
- **후속 3 — 구방식 보류 음원**: 욥 39:8 의 보류 음원은 9/9 구방식에 강제 분할까지 걸려 절 중간 문장끝(90ms)·절 끝(30ms)이 모두 잘렸다. 같은 날 '이대로 사용'한 마 1:5·눅 3:27·3:36·10:27 도 구방식 음원이었는데, 동결 시각 뒤에 올라가 새 방식 파일로 분류돼 교체에서 빠질 뻔했다(서버 음원을 지우고 새 방식으로 다시 만듦). PC 보고에 생성 방식(`method`)을 싣고, 관리자 화면은 구방식 음원에 표시를 붙이고 '이대로 사용'을 막는다. 관리자 재생성은 절을 통째로 먼저 만든다(tries 0)
- **후속 2 — 끝난 책의 본문 최신화**: 본문 최신화가 '멈춘 작업을 이어갈 때'만 돌아, 끝난 책의 작업은 정정 이전 본문을 계속 들고 있었다. 관리자 재생성 요청이 그 옛 본문(편집자 주석 포함)으로 다시 만들었고(요 1:42), 현재 본문 키에는 영희 음원이 없었다. 작업을 **시작할 때마다** 최신화하고, run_plan 도 시작할 때 모든 작업을 최신화해 만들 절이 생긴 끝난 책을 큐에 올린다(`refresh_all_jobs`)
- **후속 (같은 날)**: 음원이 여전히 하나도 안 올라갔다 — PC 가 항목 id 를 서버 규칙과 다르게 계산해 `missingAudio` 와 짝이 안 맞았다(시험은 가짜 서버가 같은 함수를 써서 못 잡음). 서버가 **보낸 목록의 위치**(`missingAudioIndex`)로 알려 주게 바꿔 PC 쪽 id 계산을 없앴다. 보류 수가 그대로여도 30분마다 다시 보고. 관리자 화면은 음원이 없으면 무조건 '괄호 때문에 만들지 않았다'고 안내하고 재생성 버튼까지 막았다 — 괄호 안내는 주석 잔재 보류에만, 재생성은 음원 없이도 가능하게

### 2026-09-10 — 한국어 성우 정리: 생생·활력 제외, 기본 성우 영희
- **무엇**: 선택 목록(`KOREAN_VOICE_ORDER`)에서 생생(f2)·활력(m2)을 뺐다. 둘 다 GCP Chirp3-HD 인데 새번역 낭독에서 띄어쓰기(끊어 읽기)가 부자연스럽다는 청취 판단. 기본 성우는 날짜 홀짝(생생/활력)에서 **영희(f4)** 로 — 모든 역본 공통
- **저장값 처리**: 예전에 생생·활력을 골라 저장한 사용자는 고른 적이 없는 것으로 보고 영희로 돌린다(`RETIRED_KOREAN_VOICES`). 저장값을 지우지는 않는다
- **서버는 그대로**: Chirp3-HD 는 유료 엔진(ElevenLabs·Supertone)이 실패할 때의 폴백으로 계속 쓰인다. 영희 음원이 없는 절(구약 미생성분·다른 역본)은 김단아(f1)가 대신 읽고 김단아 키로 캐시되므로 영희 캐시가 오염되지 않는다

### 2026-09-09 — 말씀의삶 (성경읽기진도표 91회차 북클럽) — 배포 완료
- **무엇**: 예봄교회 종이 성경읽기진도표 91회차를 앱에 옮긴 층. 하단 탭 6번째. 지시서 [docs/tasks/말씀의삶_클코_작업지시서_v1.1.md](docs/tasks/말씀의삶_클코_작업지시서_v1.1.md), 설계 [docs/READING_PLAN.md](docs/READING_PLAN.md)
- **저장하지 않고 파생한다**: 회차 완료를 별도 저장하지 않고 기존 `reading_progress`(장 단위 자동 기록)에서 매번 계산한다(`lib/plans/yebom91.ts` `computeUnitProgress`). 저장하면 두 값이 갈라지고, 갈라지면 어느 쪽이 맞는지 알 수 없다. 자동으로 알 수 없는 것 하나 — "종이로 읽었다" — 만 `reading_unit_checks` 에 담는다
- **`nextChapter` 와 `entryChapter` 를 나눈 이유**: 진도표는 한 장이 두 회차에 걸치는 **경계 장이 10개**다. "안 읽은 첫 장"을 진입점으로 쓰면 경계 장에서 고정점이 생겨 6개 회차 49장이 영원히 같은 장을 가리켰다. 표시용(`nextChapter`)과 진입용(`entryChapter`)을 분리해 끊었다
- **플랜 모드**: 회차를 열면 장 이동이 정경 순서가 아니라 **진도표 순서**를 따른다(아가 8장 → 잠언 16장). 스와이프·◀▶·여백·키보드 4경로를 `goChapter` 하나로 통합(단계 2.5)하고 그 위에 플랜 오버라이드를 얹었다. TTS 자동 진행도 같은 순서를 따른다
- **책 경계 이동**(별건, 플랜 무관): 기본 장 이동이 책 끝에서 다음 책으로 넘어간다 — 창세기 50장 ▶ 출애굽기 1장
- **그룹**: 목적은 순위표가 아니라 **동행 확인**이다. 벌점·독촉·미읽음 알림을 두지 않는다. 순위는 완료 회차 내림차순, **동률은 이름 가나다순** — `read_at` 은 재열람마다 갱신되어 이미 읽은 장을 다시 열면 순위가 뒤집힌다. 멤버 진도 조회는 `fetchAllRows` 페이지네이션 필수(1,000행 상한, 36명이면 닿는다). `standings` 는 호출자가 멤버인지 먼저 확인한다 — 없으면 `id` 를 올려가며 남의 명단이 보인다
- **DB**: `reading_unit_checks` / `reading_groups` / `reading_group_members` 3종. 전부 RLS enable + 정책 0개(service_role 전용) — anon 키로 조회해 빈 배열임을 실측 확인. [docs/DATA_SCHEMA.md](docs/DATA_SCHEMA.md) §5c
- **검증**: `npm run verify:plan`(플랜 데이터 7절), `node scripts/verify-reading-groups.mjs`(계정 4개 24항목). 프로덕션 스모크 — buildId `585cf1e`, 신규 API 4개 정상, `reading-progress` 회귀 없음
- **미결**: 실제 `말씀의삶1기` 그룹 생성(지휘부가 UI 로). 보류는 [plan.md](plan.md)

### 2026-06-12 — 뒤로가기 무확인 종료 차단 (Chrome trivial/gesture history intervention)
- **증상**: 6/11~ 로그인 상태에서 뒤로가기·메뉴 닫기 시 확인 없이 앱 밖(빈 화면/로그인 전 화면)으로 튕김. Chrome·Edge 발생, PWA standalone·모바일 면역. 로그아웃(vercel.app 직접 접속)은 정상.
- **원인**: Chrome 이 (a) *trivial session history context*(탭에 히스토리 항목 1개 — 로그인 SSO 가 bible.yebom.org 를 단일 항목으로 올림) 또는 (b) *사용자 제스처 없이 `useEffect` 에서 호출된 `pushState`*(History Manipulation Intervention)를 `replaceState` 로 강등/건너뜀 → `isAppRoot`/`isHome` 종료 트랩과 `useHardwareBack` push 가 무력화 → 뒤로가기가 앱 밖 직행. 콘솔: "Use of history.pushState in a trivial session history context … treated as history.replaceState". 6/10 코드로 되돌려도 동일(코드 회귀 아님 — Chrome 정책 변화가 방아쇠).
- **해결 (2중 안전망)**:
  - `hooks/useHardwareBack.ts`: `addedHistoryEntry` 를 `window.history.length > lenBefore`(실제 증가)만으로 판정 — `lenBefore>1` 오판정 제거. pushState 강등 시 닫을 때 `history.back()` skip → 모달 닫기·뷰전환 튕김 차단(잔여 항목 무해).
  - `app/page.tsx` + `lib/appExit.ts`(신규): **로그인 + 비-PWA 브라우저 탭**에 `beforeunload` 무조건 무장 → trivial/gesture 무관하게 무확인 종료 차단(브라우저 기본 확인창). 종료 버튼(`exitingRef`)·로그인 SSO 이동(`markIntentionalLeave`)은 통과. PWA standalone 면역이라 무장 안 함.
  - 비-trivial 컨텍스트는 기존 `isAppRoot`/`isHome` 커스텀 "종료?" 팝업 유지.
- **한계**: 로그인 브라우저 탭에선 새로고침·탭닫기도 브라우저 기본 확인창(커스텀 팝업은 trivial 에서 원천 불가). 후속(옵션): 단일 센티넬+메모리 스택으로 뒤로가기=모달닫기 UX 복원.
- **부수**: `requireAuth()` 죽은 코드 제거(무확인 로그인 리다이렉트 지뢰) + 음원 이전 실험 스크립트 4종 제거. 음원 Supabase→R2 이전(easy/nkrv/web 3567파일 8.3GB, `bible_audio.audio_url` R2 전환, 원본 마스터 보존) 반영.

### 2026-06-11 — 로그인 게이트 통일 + 로그인 전용 기능 4종 (코드 완료, DB 마이그레이션 대기)
- **게이트 보완**: `components/LoginGate.tsx` 신규 — `LoginGateProvider`(layout 마운트) + `useLoginGate().ensureLogin(label)`. 찬송가 악보 인앱 모달을 공용화. `requireAuth()` 즉시 외부 튕김(page/VerseDisplay/HymnModal) → 인앱 "로그인 필요" 모달 경유로 통일. `LOGIN_URL` 단일화(`useSession.ts` export)
- **401 피드백**: CardPreview 카드저장 거짓 성공 안내 수정 + 사진삭제 실패 중단, ScrapList 삭제 실패 롤백+alert
- **A 통독진도**: `reading_progress` 테이블 + `/api/reading-progress` + `lib/reading-progress.ts`(computeProgress) + `lib/books.ts` `CHAPTER_COUNTS`(1189장). SearchPanel 3초 트리거에 `markChapterRead` 합류, 책갈피 메뉴에 전체/구약/신약 진도 바
- **B 묵상노트·하이라이트**: `verse_notes` 테이블 + `/api/verse-notes` + `lib/verse-notes.ts`(4색). renderVerseItem 하이라이트 배경+메모 표시, 하단 액션바 [형광펜][메모], 메모 에디터 모달
- **C 카드갤러리**: 신규 테이블 0. ScrapList `myCards` 탭 — `image_url` 있는 스크랩 그리드 + 라이트박스(확대/재다운로드)
- **E 기기간 동기화**: `user_state` 테이블 + `/api/user-state` + `lib/userSync.ts`. 책갈피 union 머지 + 마지막위치(recent) last-write-wins. 로그인 시 1회 동기화 + 변경 시 푸시
- **마이그레이션**: `scripts/migration-login-features.sql` (3개 테이블, RLS enable+정책없음=service-role 전용) — **적용 대기**
- 빌드 통과(`npx next build`). [plan.md](plan.md) "로그인 게이트 보완" 항목 참조

### 2026-06-11 — AI 주제 추천 모든 version 호환
- 증상: mainVersion=KJV 에서 "추천된 구절을 DB에서 찾을 수 없습니다" 에러
- 원인: AI 가 한국어 책명("사도행전")만 반환 → SearchPanel 이 `book_name='사도행전'` 으로 KJV(`book_name='Acts'`) 조회 → 0건
- 수정: `lib/books.ts` 에 `getBookByName(name)` 신규 (한글/영문/약어/code 모두 인식). SearchPanel topic 조회를 `book_code` 매칭으로 변경
- 결과: KJV/NIrV/GNT/WEB 등 영문 mainVersion 에서도 주제 추천 정상 동작
- plan.md D 항목 완료 처리

### 2026-06-11 — 문서 체계 재정비
- architecture.md 를 living document 로 재구성. 변경 이력 누적 시작
- [documents.md](documents.md) 신규 — 문서 체계 단일 인덱스
- 실효 문서 16건 → [backup/](backup/) 격리
- [AGENTS.md](AGENTS.md) + [docs/TTS_PIPELINE.md](docs/TTS_PIPELINE.md) + [docs/IA_5TAB.md](docs/IA_5TAB.md) + [docs/DATA_SCHEMA.md](docs/DATA_SCHEMA.md) 신규
- 기존 architecture.md v0.4.0 → [backup/architecture-v0.4.0.md](backup/architecture-v0.4.0.md) 이동

### 2026-06-11 — useSession race condition fix
- `hooks/useSession.ts`: `requireAuth()` 에 `loading` 가드 추가. 세션 fetch 미완료 중 외부 로그인 페이지로 점프하던 회귀 차단
- `pageshow` (event.persisted) 리스너 추가 — bfcache 복원 시 세션 자동 재검증
- `app/page.tsx`: `handleCreateCard` / `onOpenScrap` 가 `sessionLoading` 일 때 "로그인 확인 중..." 토스트 안내

### 2026-06-11 — 미니플레이어 single-toggle 패턴 + 0.7x 속도
- 미니플레이어 컨트롤 재구성: `[▶] [ref+bar] [AI/녹음] [속도] [발음] [음성] [⋮] [✕]`
- 발음(`미국식` ↔ `영국식`) / 음성(`여` ↔ `남`) single-button 즉시 전환 토글
- 설정창은 두 옵션 모두 노출(segmented), 플레이어는 현재 값만 노출 — 패턴 분리
- 재생 속도 `0.7x` 추가 (영문 청취 학습용). 7단계 그리드
- 누름 영역 ~2배 확대 (px-1 → px-3, text-9px → 11px)
- 본문 하단 가림 보정 — 4개 verse 스크롤 컨테이너 max-h 에 `env(safe-area-inset-bottom)` 동적 가산

### 2026-06-11 — 미니플레이어 라벨 단순화 + 캐시 키 accent 분기
- `Chirp/Neural2/Wavenet` → 단일 `AI` 라벨 (사용자 인지 통합)
- `KOR/ENG` 라벨 제거 (발음 토글 존재 자체로 영문 컨텍스트 암시)
- `ttsCache.makeCacheKey` 에 `accent` 필드 추가 → 같은 절도 미국식·영국식 별도 캐싱

### 2026-06-11 — 설정 시트 X 버튼 fix
- 그립 드래그 영역(`setPointerCapture`) 내부 X 버튼 click 가로채짐 해소
- `onPointerDown={(e) => e.stopPropagation()}` 추가 패턴

### 2026-06-10 — 영문 발음 분기 (en-US / en-GB)
- `/api/tts`: `accent` 파라미터 수용. `en-GB-Chirp3-HD-Aoede/Charon` voice 추가
- `TtsContext`: `englishAccent` state + `localStorage["yebom_tts_english_accent"]` 영속
- SettingsSheet 음성 섹션: `[여성][남성]` + `[미국식][영국식]` segmented 신설
- `DB_VERSION` 4→5 bump

### 2026-06-10 — 스크랩 중복 row 차단
- DB: `scraps_unique_per_user` UNIQUE `(user_id, book_code, chapter, verse_start, version)` 추가 + 기존 중복 20+ 그룹 정리
- API: SELECT-then-UPDATE/INSERT 패턴으로 재작성. UNIQUE 있든 없든 안전
- `handleSelectScrap` 가 `setMainVersion(version)` 동기화 — version 키 달라서 새 row 생성 차단

### 2026-06-10 — WEB 영문 음원 (Williams + R2)
- 1189장 mp3 (1.33 GB, 48 kbps mono) Cloudflare R2 bucket 적재
- `bible_audio` 1189 row upsert. 코드 변경 0줄 (TtsContext 이미 version 무관 동작)
- 영문 mainVersion=web 선택 시 "녹음" 배지 + 사람 낭독 mp3 자동 재생

### 2026-06-10 — WEB 역본 31,098절 + 영문 그룹 최상위
- `bible_verses` version=`web` 적재 (Classic, Yahweh 표기)
- 셀렉터 순서: 한국어 3종 → WEB → KJV → NIrV → GNT (WEB 영문 그룹 최상위)
- `EnglishVersion` 타입 + 6개 하드코딩 배열 일관 갱신

### 2026-06-10 — 영문 TTS voice 자동 분기 + 캐시 무효화
- `/api/tts`: `lang` 파라미터 수용. `isEnglishVersion(track.version)` 자동 판정
- 영문 역본(KJV/NIrV/GNT/WEB) → `en-US-Chirp3-HD` voice
- 장 안내 announcement: 영문이면 "Psalms chapter 23" 형식
- 절 prefix: 영문이면 "Verse 16." 형식
- `DB_VERSION` 3→4 bump

### 2026-06 — 글꼴 조정 미리보기 (Kindle 패턴)
- SettingsSheet 글꼴 섹션: 미리보기 카드 + 압축 바 모드
- 압축 모드: 본문 보이는 채로 사이즈/폰트 즉시 변경

### 2026-06 — 5탭 IA Phase 3 완료
- 시스템 다크 추종 (`FontContext.theme: "system"`)
- 그립 드래그 시트 닫기 (BottomSheet 패턴)
- 길게 누름(500ms) → 전체화면 진입
- QuickNavFab 우상단 고정 + 더블탭 위치 토글

### 2026-06 — 5탭 IA Phase 2 (a/b/c)
- 하단 `BottomTabBar` (목차/검색/읽기/책갈피/설정) 도입
- 통합 `SettingsSheet` — 우상단/하단 FAB 제거. 찬송가/예배성경/카드빌더/풀스크린/계정 흡수
- 상단 4탭(성경목차/본문검색/주제추천/책갈피) → 검색 세그먼트 + 책갈피 탭으로 흡수
- 좌하단 스크랩 FAB → 책갈피 메뉴 안 "스크랩" 버튼
- 좌우 스와이프 장 이동

### 2026-06 — 시각 토큰 시스템 (Phase 1)
- `--paper / --canvas / --paper-2 / --ink / --amber / ...` 토큰 도입
- amber 강조 색상 시스템
- sub 번역본 세로줄 (모바일 only)

---

## 1. 시스템 개요

### 1.1 서비스 정의

**예봄성경** — 한글·영문 7개 역본을 검색·낭독·카드로 만들 수 있는 PWA 웹 서비스.

- 본문 검색·열람 (목차·단어·주제 추천)
- 카드 만들기 (배경 이미지 + 절 텍스트 → PNG 다운로드)
- 사람 녹음 음원 + TTS 합성 통합 재생
- 스크랩 (개인 + 커뮤니티)
- 책갈피 + 최근 읽음 자동 추적
- 풀스크린 리더 모드

### 1.2 기술 스택

| 영역 | 선택 |
|---|---|
| Framework | Next.js 16 (App Router) + React 19 |
| Style | Tailwind v4 (`@theme inline` + CSS 변수 토큰) |
| State | React Context (Tts, Font) + useState/useReducer |
| DB | Supabase Postgres |
| Storage | Supabase Storage (한글 음원) + Cloudflare R2 (영문 음원) |
| Auth | iron-session (cookie) + saint.yebom.org SSO |
| TTS | GCP Cloud TTS (Chirp3-HD → Neural2 → Web Speech 3단 폴백) |
| AI | AI Gateway (이미지 생성/편집/주제 추천) |
| Deploy | Vercel (master push 자동 배포) |

### 1.3 외부 의존성

| 서비스 | 용도 | 가이드 |
|---|---|---|
| Supabase | DB + Storage(easy/nkrv 음원) | [성경데이터확장문서.md](성경데이터확장문서.md) |
| saint.yebom.org | SSO 로그인 hub | [AUTH_INTEGRATION_GUIDE.md](AUTH_INTEGRATION_GUIDE.md) |
| Cloudflare R2 | WEB 영문 음원 호스팅 | [docs/TTS_PIPELINE.md](docs/TTS_PIPELINE.md#7-web-영문-음원-williams--r2) |
| GCP Cloud TTS | 한국어/영문 합성음 | [docs/TTS_PIPELINE.md](docs/TTS_PIPELINE.md) |
| AI Gateway | 이미지 생성/편집/추천 | [CLIENT_INTEGRATION_GUIDE.md](CLIENT_INTEGRATION_GUIDE.md) |

---

## 2. 컴포넌트 구조

### 2.1 폴더 레이아웃

```
app/                  Next.js App Router
  page.tsx              메인 상태 머신 (view, modal, 인증)
  api/
    tts/                Cloud TTS 프록시
    scrap/              스크랩 CRUD
    photos/             카드 배경 이미지
    auth/session        세션 조회
    ai/recommend        AI 주제 추천
  share/                공유 페이지 (절 + URL 파라미터)

components/
  SearchPanel             검색·목차·읽기 통합 (2000+ 줄)
  VerseDisplay            선택 절 표시 + 카드 만들기 진입
  CardPreview             카드 미리보기 + PNG 다운로드
  CardBuilder             다중 책 절 선택 빌더
  FullscreenReader        풀스크린 리더
  TTSMiniPlayer           재생 컨트롤 (하단 고정)
  BottomTabBar            5탭 네비 (목차/검색/읽기/책갈피/설정)
  SettingsSheet           통합 설정 시트
  ScrapList               스크랩 목록 (나의/커뮤니티)
  HymnModal               찬송가
  WorshipBible            예배성경
  QuickNavFab             우상단 빠른 네비

contexts/
  TtsContext              TTS 큐·재생·캐시
  FontContext             글꼴·테마(시스템 다크 추종)

hooks/
  useSession              인증 (loading 가드 + bfcache)
  useHardwareBack         모달 스택 + popstate
  useWakeLock             화면 켜짐 유지

lib/
  supabase                anon 클라이언트
  supabaseAdmin           service role (API only)
  scrap                   스크랩 fetch/CRUD
  books                   책 메타 (한글/영문 명, 약어)
  parseReference          한글 책명 → book_code
  versions                getVersionLabel + isEnglishVersion
  bookmark                책갈피 localStorage
  bibleAudio              bible_audio 매핑 lookup
  tts/
    cloudTtsClient          /api/tts 호출
    webSpeechClient         Web Speech 폴백
    ttsCache                IndexedDB 캐시
  auth/session            iron-session 정의

scripts/                  운영 1회성 (upload-*, gen-*, verify-*)

docs/                     시스템 단면 상세
backup/                   실효 문서 보관
```

### 2.2 핵심 패턴

**상태 머신 (`app/page.tsx`)**:
- `view: "search" | "display" | "card" | "scrap"` + 다수 modal boolean
- 각 view·modal 에 `useHardwareBack` 등록

**모달 스택 (`hooks/useHardwareBack`)**:
- 전역 `modalStack` 배열 + 단일 popstate listener
- 모달 push 시 history.pushState({modalId})
- UI 닫기 시 history.back() (skipPopstateCount 로 listener 회피)
- 상세: [docs/IA_5TAB.md §4](docs/IA_5TAB.md)

**TTS 큐 (`contexts/TtsContext`)**:
- `queueRef` + `playGenRef` (race 차단)
- 절 단위 트랙 + 장 안내 announcement + mp3 통째 트랙 (음원 모드)
- 캐시 → fetch → 폴백 3단
- 상세: [docs/TTS_PIPELINE.md](docs/TTS_PIPELINE.md)

**인증 (`hooks/useSession` + `components/LoginGate`)**:
- `/api/auth/session` 1회 fetch (mount) + bfcache 시 재검증
- `useLoginGate().ensureLogin(label)` 가 `loading` 가드 → race 차단
- 미로그인 시 인앱 "로그인 필요" 모달 → "로그인" 클릭 시에만 saint.yebom.org 외부 이동
- 상세: [AUTH_INTEGRATION_GUIDE.md](AUTH_INTEGRATION_GUIDE.md)

---

## 3. 데이터 흐름

### 3.1 본문 조회

```
사용자: 책·장 선택 (목차/책갈피/QuickNav)
  ↓
SearchPanel: setMainVersion + setBookCode + setChapter
  ↓
useEffect → Supabase SELECT bible_verses WHERE version=main+book+chapter
  ↓
browseVerses 상태 갱신 → 화면 렌더
```

### 3.2 절 선택 → 카드 만들기

```
사용자: 절 클릭 (단일/다중)
  ↓
handleToggleVerse → selectedVerses 추가/제거
  ↓
사용자: "이 말씀으로 카드 만들기"
  ↓
handleCreateCard:
  1. ensureLogin("카드 만들기") 가드 (미로그인 시 인앱 모달)
  2. addScrapToServer(selectedVerses, mainVersion)
     → API: SELECT-then-UPDATE/INSERT (scraps)
  3. setView("card") → CardPreview
  4. CardPreview: AI 배경 추천 → PNG 합성 → 다운로드
```

### 3.3 TTS 재생

```
사용자: 미니플레이어 ▶ 또는 본문 길게 누름
  ↓
SearchPanel.handleTtsToggle → buildTtsTracks(browseVerses)
  ↓
tts.start({tracks, loadNextChapter})
  ↓
TtsContext.playIndex(0):
  1. transformWithChapterAudio: bible_audio lookup → mp3Url 채움
  2. injectChapterAnnouncements: "Genesis chapter 1" 안내 삽입
  3. 트랙별:
     - mp3Url 있으면 <audio> 직재생
     - 없으면 IndexedDB cache get
        - hit → blob 재생
        - miss → /api/tts POST {text, voice, speed, lang, accent}
                → blob 캐시 + 재생
                → 401/500 시 WebSpeechController.speak() 폴백
```

### 3.4 인증 흐름

```
페이지 mount
  ↓
useSession effect: fetch /api/auth/session
  ↓
응답: { session: { isLoggedIn, user_id, name } | null }
  ↓
loading=false, session=값 또는 null

사용자: 보호 액션 (스크랩/카드/링크)
  ↓
ensureLogin(label):
  - loading=true → false 반환 ("로그인 확인 중..." 토스트)
  - session.isLoggedIn → true 반환 → 액션 진행
  - 그 외 → 인앱 "로그인 필요" 모달 ("로그인" 클릭 시에만 saint.yebom.org/login?from=bible 이동)

bfcache 복원 (모바일 백그라운드 → 포그라운드)
  ↓
pageshow event.persisted=true
  ↓
useSession refresh() → /api/auth/session 재조회 → loading 잠시 true → 응답 도착
```

---

## 4. 데이터 모델

상세 + UNIQUE 정책 + 마이그레이션 이력은 [docs/DATA_SCHEMA.md](docs/DATA_SCHEMA.md) 참조.

핵심 테이블:
- `bible_verses` — 본문 (7 version × ~31,000절)
- `bible_audio` — 장 단위 음원 매핑 (3 version × 1,189장)
- `scraps` — 사용자 스크랩 (UNIQUE per user)
- `user_photos` — 카드 배경

클라이언트 영속:
- `localStorage["yebom_main_version"]`, `_sub_version`, `_tts_voice`, `_tts_speed`, `_tts_english_accent`, `_tts_auto_next`, `_tts_read_verse_number`, `_bookmarks`, `_quicknav_pos`
- `IndexedDB["yebom_tts_cache"]` (DB_VERSION 5)

---

## 5. 환경 변수

```bash
# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://....supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=...
SUPABASE_SERVICE_ROLE_KEY=...

# 인증
SESSION_SECRET=...                  # iron-session

# TTS
GCP_SERVICE_ACCOUNT_JSON=...        # base64 인코딩

# WEB 영문 음원 (R2)
R2_ACCOUNT_ID=...
R2_ACCESS_KEY_ID=...
R2_SECRET_ACCESS_KEY=...
R2_BUCKET=attached
R2_PUBLIC_BASE=https://pub-....r2.dev

# AI Gateway (CLIENT_INTEGRATION_GUIDE 참조)
AI_GATEWAY_URL=...
AI_GATEWAY_KEY=...
```

---

## 6. 배포 + 운영

- **빌드**: `npx next build`
- **로컬 dev**: `npm run dev`
- **Vercel**: master push 시 자동 배포 (webhook)
- **DB 변경**: Supabase MCP 또는 Dashboard SQL 에디터
- **음원 적재**: `scripts/upload-*.mjs` 로컬 실행 (자격증명 .env.local)
- **캐시 무효화**: `lib/tts/ttsCache.ts` `DB_VERSION` bump → 다음 사용자 접속 시 IndexedDB 재생성

---

## 7. 위험 패턴 (사전 차단)

| 패턴 | 회피 |
|---|---|
| 로그인 게이트에 `loading` 미체크 | `useLoginGate().ensureLogin()` (loading 가드 내장) |
| history `pushState` 트랩만으로 뒤로가기 종료 차단 | Chrome trivial/gesture intervention 으로 강등 → `beforeunload` 안전망 병행 |
| pointer capture 안에 click 버튼 | `onPointerDown stopPropagation` |
| `scraps` INSERT (UNIQUE 미인지) | API SELECT-then-UPDATE 패턴 |
| TTS 영문 본문 ko-KR voice | `lang="en"` 자동 분기 |
| 캐시 키 신규 분기 누락 | `makeCacheKey` 확장 + `DB_VERSION` bump |
| `BibleVersion` 신규 추가 시 배열 누락 | 6+ 위치 일관 갱신 ([docs/DATA_SCHEMA.md §9](docs/DATA_SCHEMA.md#9-bibleversion-신규-추가-체크리스트)) |

---

## 8. 관련 문서

| 문서 | 역할 |
|---|---|
| [documents.md](documents.md) | 문서 체계 인덱스 + 유지보수 규칙 |
| [AGENTS.md](AGENTS.md) | AI 개발 보조 작업 규칙 |
| [plan.md](plan.md) | 보류·차기 검토 단일 누적 |
| [docs/TTS_PIPELINE.md](docs/TTS_PIPELINE.md) | TTS 단면 |
| [docs/IA_5TAB.md](docs/IA_5TAB.md) | 5탭 IA + 모달 스택 |
| [docs/DATA_SCHEMA.md](docs/DATA_SCHEMA.md) | Supabase 스키마 |
| [AUTH_INTEGRATION_GUIDE.md](AUTH_INTEGRATION_GUIDE.md) | SSO 계약 |
| [CLIENT_INTEGRATION_GUIDE.md](CLIENT_INTEGRATION_GUIDE.md) | AI Gateway 계약 |
| [성경데이터확장문서.md](성경데이터확장문서.md) | 한글 역본 적재 가이드 |
